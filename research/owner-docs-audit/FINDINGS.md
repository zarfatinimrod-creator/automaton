# Owner-facing documents — claim check (Opus half)

Nine Opus checkers (workflow `wf_a170db68-c5f`, 25.9.2026) extracted every checkable claim from the documents the owner
acts on and checked each against the repository only. Checked against commit `245a19e`, **before** the 22.9 post-merge work
was merged in (`635a0d1`). The column "now" is mechanical: `present` = the claim's verbatim text is still in the document
at HEAD; `changed` = it is not (the merge or a later edit touched it; re-verify before acting on the finding).

**Nothing here has been applied.** Per the model rule, each WRONG/STALE/UNSUPPORTED finding is refuted by a separate Fable
agent before any document changes (queued; Fable quota exhausted 25.9 ~16:00 UTC). Full per-document reports alongside.

**Totals:** 851 claims checked; findings: STALE 39, UNSUPPORTED 42, WRONG 95; HIGH owner impact: 22; claim text changed since the check: 12.

## `docs/INCOME_PLAN.he.md` — 118 claims checked, 42 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 9 | תרחיש סביר: חודשים 1–3 כמעט אפס; חודשים 4–6 ₪1,000–8,000; חודשים 9–12 ₪5,000–20,000 אם 2–3 קווים תופסים. | present | research/colony-sweep/CHIEF-AUDIT.md:123 'Sum: ₪2,200/month at 12 months (₪3,500 if Apify's contested ₪1,500 held). Month one: ₪0 on every line'; portfolio.ts:16-25 board committed ₪1,500; portfolio.ts:280 Apify first ledger entry ~month 9; portfolio.ts:292 il-biz-tools ₪0 through month 12. The ₪50,000 'breakthrough' paths in the same sentence are a ₪200 target and a killed plugin line (portfolio.ts:453, CHIEF-AUDIT.md:136 WordPress ₪0–200). | התרחיש המבוקר: ₪0 בחודש הראשון בכל קו; תקרה מבוקרת כ-₪2,200 לחודש אחרי 12 חודשים (₪3,500 אם הגבול השנוי במחלוקת של Apify יחזיק); הדירקטוריון התחייב ל-₪1,500. אף קונה לא נמדד, ו-₪20,000 לא נתמך בשום ראיה. |
| **HIGH** | STALE | 236 | מה שבא במקומה: אותם תשעה קווים, מדורגים לפי **מה שנשאר אחרי שהמבקר עבר** | present | src/revenue/portfolio.ts:34-203 DEFAULT_PORTFOLIO = 4 lines (apify-actors ₪200, il-biz-tools ₪400, oss-bounties ₪300, pcn874 ₪600); portfolio.ts:396-470 KILLED_LINES = templates, paid-apis, agent-services, telegram-bots, dev-extensions, hebrew-content, killedOn 2026-09-07; colony.db status 'killed' for all six. Rows 4-9 of the §3 'ranked' table (L244-249) are killed lines; pcn874, the largest committed target, is absent; row 1 'חלק מ-₪800' vs target ₪300 (portfolio.ts:163). | Replace the table with the board's four: \| 1 \| Bounties (Algora) \| ₪300 \| inferred, קוד \| … \| 2 \| PCN874 \| ₪600 (טווח ₪300–600) \| inferred \| … \| 3 \| Apify Actors \| ₪200 (גבול עליון שנוי במחלוקת ₪1,500) \| inferred \| … \| 4 \| il-biz-tools \| ₪400 \| contradicted \| … — סה״כ ₪1,500 מחויב + ₪700 מותנה; ששת הקווים שנהרגו עוברים ל-§4 עם הפניה ל-`docs/REJECTED.md` "Board decision, 7.9.2026". |
| **HIGH** | STALE | 299 | \| 1 \| למזג את PR #2 \| 2 \| הסכמה, לא זהות: מעביר את הלולאה ל-`main`. | present | `git log origin/main --format='%h %ad %an %s' --date=iso` -> `31cda66 2026-09-22 21:39:15 +0300 Nimrod zarfati Merge pull request #2 — the revenue colony, six products, the criteria sweep and the board's decision`. `git show origin/main:.github/workflows/colony.yml` succeeds; 16 colony-bot 'colony tick' commits since 2026-09-22 18:40:14 UTC. Step 1 is done; the doc still lists it first and so never tells the owner the Apify half of step 6 (L294-295 'אפשר לעשות מיד אחרי צעד 1') is open now. (logs/CHECKPOINT.md 22.9 18:20 UTC 'PR #2 פתוח' predates the merge by 19 min.) | \| 1 \| למזג את PR #2 \| ✅ בוצע 22.9.2026 (`31cda66`) \| הלולאה על main ורצה. הצעד הבא: 2; חצי ה-Apify של צעד 6 פתוח כבר עכשיו \| |
| **MEDIUM** | UNSUPPORTED | 7 | הלופ רץ מה-heartbeat: סנכרון כסף כל שעה, ביקורת מפקח כל 6 שעות, ישיבת דירקטוריון יומית, ביקורת שבועית. | present | Intervals are correct (src/revenue/heartbeat.ts:54-57). But the Automaton heartbeat runs only under `node dist/index.js --run`, which exits without a Conway key (src/index.ts:199-202). What runs is colony.yml -> `pnpm exec tsx scripts/colony.ts tick --no-feed` (colony.yml:67), governance only: 'No orchestrator runs here… Governance runs; execution waits' (colony.yml:64-66). Observed cadence ~3–6 h, not hourly (16 commits 22.9 18:40 -> 25.9 13:44; the commit step fires every run since REPORT.md is re-stamped). | מחזור הממשל רץ מ-GitHub Actions (`colony.yml` → `scripts/colony.ts tick`) על main מאז 22.9.2026: סנכרון כסף, ביקורת מפקח, דירקטוריון, ביקורת — כל אחד לפי המרווח שלו. ביצוע (דירקטורים ועובדים) עדיין לא רץ: הוא דורש את ה-runtime של האוטומטון. |
| **MEDIUM** | WRONG | 8 | 9 קווים (2 ליבה, 2 צמיחה, 5 ניסויים), יעד מצטבר ₪25,000/חודש | present | portfolio.ts:34-203: 4 lines, 3 core + 1 growth, sum ₪1,500 (state/colony/REPORT.md 'Line targets, summed ₪1,500'). The original nine summed to ₪16,500 (CRITIC-synthesis.md:195-197; the doc's own L43-44); colony.db tiers of the original nine: 2 core / 3 growth / 4 experimental. '25,000' appears in no repo source. | **תיק ההכנסות**: 4 קווים (3 ליבה, 1 צמיחה) אחרי פסק הדירקטוריון 7.9.2026, יעד מצטבר ₪1,500/חודש + ₪700 מותנים. התיק המקורי (9 קווים) הסתכם ב-₪16,500. |
| **MEDIUM** | WRONG | 10 | הרשימה המינימלית בסעיף 6 — כ-2–3 שעות עבודה סה"כ, ואחריה הכל אוטומטי. | present | Hours are right (owner-steps.ts minutes sum 132–172). 'Everything automatic after' is contradicted by the doc's own L311-312 and docs/OWNER_STEPS.he.md:262-271 (Apify KYC, PayPal, Stars/USDC identity checks later); owner-steps.ts:106,132 (עוסק מורשה switch and every domain renewal are owner decisions); .github/workflows/colony.yml:7-9 'Director and worker EXECUTION still needs the automaton runtime'. | …כ-2–3 שעות עבודה סה"כ. אחריה: הממשל רץ לבד (דוח ב-`state/colony/`); צעדי זהות נוספים רק אם קו מתחיל להרוויח (סוף `docs/OWNER_STEPS.he.md`), וחידוש הדומיין הוא החלטה שלך בכל שנה. |
| **MEDIUM** | STALE | 31 | אין קובץ `state/`, אין בסיס נתונים של לדג'ר, ומעולם לא נכתבה אליו שורה. | present | `git ls-files state` -> state/colony/REPORT.md, state/colony/colony.db, state/colony/dashboard.html; 16 colony-bot ticks since 2026-09-22. Read-only query `SELECT count(*) FROM revenue_ledger` -> 0, so the ₪0 conclusion stands but the reason is obsolete. | **והיום זה ₪0.** הלדג'ר קיים (`state/colony/colony.db`) והלולאה רצה על main מאז 22.9.2026, אבל לא נכתבה אליו אף שורה. |
| **MEDIUM** | UNSUPPORTED | 74 | העלות שלך: onboarding self-serve, בלי שיחה, בלי מצלמה, בלי הוצאה. | present | No Stripe Connect Express requirements page is rendered anywhere in the repo; stripe.com is EGRESS_BLOCKED (docs/REJECTED.md:1343). CHIEF-AUDIT.md §4A.3 says only 'identity document, address, Israeli bank account. Self-serve.' 'No camera' is asserted, not found; a camera step would be a mandate collision (BOARD.md:242 planned a screen for exactly that). | …onboarding self-serve (ת״ז, כתובת, חשבון בנק). אם Stripe מבקש סלפי או וידאו — עצור ודווח; זו התנגשות עם המנדט. |
| **MEDIUM** | WRONG | 90 | זה **לא סגור**, וזו אחת מארבע הבדיקות של שלושים השניות למטה. | present | `grep -n "שלושים\\|שניות" docs/INCOME_PLAN.he.md` -> only line 90; there are no 'thirty-second checks' below. Paddle is no longer a step at all: rails.ts:136-147 (status 'option', chosenBy 'the owner, and only the owner'). | זה **לא סגור**, והוא לא צעד שלך: Paddle היא אפשרות בלבד, עם שלושה סיכונים ב-`src/revenue/rails.ts`. |
| **MEDIUM** | STALE | 94 | **Freemius** — ישראלית במקור, לפי הדיווח משלמת בשקלים בלי עמלת המרה. **ברמת snippet בלבד**, ראה משימה #21. | present | Task #21 closed 7.9.2026. rails.ts:352: "The earlier 'pays in ILS, no conversion fee' claim stays REFUTED as stated"; PAYS ISRAEL = YES rendered (freemius supported-countries.txt line 308); ILS sales converted to a USD balance, ILS payout only via Wise/wire; fee 4.7% + ~3.5% ≈ 8.2%. docs/REJECTED.md:1265; logs/CHECKPOINT.md:104. | **Freemius** — משלם לישראל (מרונדר 7.9.2026); היתרה בדולרים, תשלום בשקלים רק דרך Wise או העברה בנקאית; עמלה כ-8.2%. הטענה "משלם בשקלים בלי עמלת המרה" הופרכה. |
| **MEDIUM** | WRONG | 95 | **קריפטו (USDC/x402, TON)** — היחיד שהאוטומטון שולט בו בעצמו, בלי KYC ובלי צד שלישי שיכול לסגור אותו. | present | Same evidence as L96: TON/Stars withdrawal via Fragment needs ID + selfie (portfolio.ts:441); crypto→ILS needs VASP KYC (audits/israel-bureaucracy.md:482-486); both x402 lines were killed 7.9 (portfolio.ts:410-433). | קריפטו (USDC/x402) — הארנק בשליטת האוטומטון, אבל ההמרה לשקלים עוברת KYC; קווי x402 נהרגו 7.9 והמסילה בהמתנה בלבד. |
| **MEDIUM** | WRONG | 96 | רק x402/USDC ו-Telegram Stars עובדים בלי שום KYC. | present | portfolio.ts:441 and BOARD.md:311: Fragment (Stars) payout KYC = ID plus selfie. audits/israel-bureaucracy.md:482-486: the supervisor's 'x402 — no account, no KYC, NO BLOCKER' is 'Contradicted by Apify's own terms … and silent on crypto→ILS settlement (Israeli VASP KYC, bank source-of-funds)'. docs/OWNER_STEPS.he.md:271: 'Telegram Stars / USDC … ואז יש אימות זהות נוסף'. | אין מסילה בלי KYC: Fragment (Stars) דורש ת״ז + סלפי, והמרת USDC לשקלים עוברת KYC של נותן שירות קריפטו. |
| **MEDIUM** | UNSUPPORTED | 97 | YouTube מסיר ערוצי AI תבניתיים | present | research/rendered/youtube-monetization-policies.txt:130 template-made channels 'are not allowed to monetize'; :156 'monetization may be removed from your entire channel'; removal is for Community Guidelines violations (:106). docs/REJECTED.md:1316 says 'penalised and demonetised'. Same error at L262 ('נענשים ומוסרים ב-2026'). Live question: the owner sent a faceless-YouTube reel 25.9 (logs/CHECKPOINT.md:7-45). | YouTube לא מאפשר מונטיזציה לערוצים שנראים עשויים מתבנית (מדיניות "inauthentic content", מרונדרת); זו הסרת מונטיזציה, לא הסרת ערוץ. |
| **MEDIUM** | WRONG | 99 | הקוד תומך גם ב-OpenAI/Anthropic/Ollama ישירות, אז הלופ לא תלוי ב-Conway. | present | src/index.ts:199-202: `const apiKey = config.conwayApiKey \|\| loadApiKeyFromConfig(); if (!apiKey) { logger.error("No API key found. Run: automaton --provision"); process.exit(1); }` — the runtime cannot start without Conway even though inference supports other providers (src/inference/registry.ts, src/ollama/). Only the governance tick is Conway-free (colony.yml:7). | מחזור הממשל (`scripts/colony.ts`) לא תלוי ב-Conway. ה-runtime המלא (`--run`) כן: הוא יוצא בלי מפתח Conway (`src/index.ts:199-202`). |
| **MEDIUM** | WRONG | 103 | התקרה הכנה של כל שישה השורדים יחד: **₪4,000–7,000 לחודש בבגרות** | present | research/colony-sweep/audits/israel-bureaucracy.md:58-62: 'The merged "₪4,000–7,000/month at maturity" headline is the sum of six unanchored numbers, which is worse than any one of them. Corrected group figure: ₪0 today; ₪0 in month one; ₪500–1,500/month'. The doc's own §1ב table (L21) uses 500–1,500. | המפקח כתב ₪4,000–7,000; המבקר תיקן ל-**₪500–1,500 לחודש בבגרות, ₪0 בחודש הראשון**. |
| **MEDIUM** | UNSUPPORTED | 128 | השער שנשאר הוא מהדורה חדשה — `pcn874-spec-watch.yml` מגבב את שלושת המקורות כל שבוע. | present | Spans L127-128. The committed lock is empty: products/pcn874/docs/SPEC-SOURCES.lock.json -> "sources": {}. The workflow has `permissions: contents: read` and no commit step (.github/workflows/pcn874-spec-watch.yml:30-53). products/pcn874/scripts/spec-watch.mjs:124-126 prints `new` and exits 0 for any source without a stored hash, so every run is a 'first download' and a new edition can never fail the job; unreachable sources also exit 0 (spec-watch.mjs:150-157). The guard the doc relies on cannot fire. | …`pcn874-spec-watch.yml` מוריד את שלושת המקורות כל שבוע, אבל עד שה-hash הראשון נשמר בקובץ ה-lock בריפו הוא לא יכול לזהות מהדורה חדשה. |
| **MEDIUM** | STALE | 137 | פרסום חינמי תחת המותג ומכירה ב-Gumroad — הוא צעדים 3 ו-4ב של הבעלים. | present | No step '4ב' exists; step 4 is Stripe Connect via Algora (owner-steps.ts:148-157). pcn874 is unlocked by steps 3 and 7 (owner-steps.ts:116,142; portfolio.ts:197-200). '4ב' was the machine-account step under a retired numbering (logs/CHECKPOINT.md:228). | …הוא צעדים 3 (Gumroad) ו-7 (ארגון GitHub + חשבון מכונה) של הבעלים. |
| **MEDIUM** | WRONG | 246 | \| 6 \| בוטים בטלגרם ב-Stars \| לא נמדד \| ללא ראיה \| חסום על שאלה אחת: האם משיכת Fragment פתוחה לתושב ישראל. | present | portfolio.ts:441 (KILLED_LINES telegram-bots, 2026-09-07): 'A MANDATE COLLISION, not a weak ceiling. Fragment's payout KYC is ID plus SELFIE — a camera step the owner's brief forbids'; research/colony-sweep/BOARD.md:106. | Remove the row; in §4: בוטים בטלגרם ב-Stars — נהרג 7.9: ה-KYC של Fragment דורש סלפי; נפתח רק אם יתרנדר משיכה לתושב ישראל בלי מצלמה. |
| **MEDIUM** | STALE | 251 | **הסכום המבוקר, אחרי ניכוי כפילויות: ₪4,650–5,650 לחודש בבגרות.** | present | That was the seven-group figure of 4.9 (CRITIC-synthesis.md:163). Whole 15-group chief audit: ₪2,200/month (CHIEF-AUDIT.md:123); board committed ₪1,500 (portfolio.ts:16). docs/OWNER_STEPS.he.md tells the owner ₪2,200. | **הסכום המבוקר של כל 15 הקבוצות: כ-₪2,200 לחודש בבגרות (ביקורת ראשית); הדירקטוריון התחייב ל-₪1,500.** (₪4,650–5,650 היה הסכום אחרי שבע קבוצות, 4.9.) |
| **MEDIUM** | WRONG | 323 | node dist/index.js --run             # הלופ מתחיל; ישיבת הדירקטוריון הראשונה זורעת את התיק | present | src/index.ts:189-202: first run launches the interactive setup wizard, then exits if no Conway API key. The loop that actually runs is `pnpm exec tsx scripts/colony.ts tick` (colony.yml:67; logs/CHECKPOINT.md:696), which §7 never mentions. | pnpm exec tsx scripts/colony.ts tick   # מחזור ממשל אחד מקובץ SQLite, בלי ארנק ובלי Conway (כך רץ ב-CI) — ו-`--run` רק עם מפתח Conway וארנק. |
| **MEDIUM** | STALE | 330 | קו x402 הוא היחיד שאינו דורש שום חשבון אנושי, ולכן הוא מקבל מוצר אמיתי ראשון (`products/x402-il-api`) | present | paid-apis and agent-services killed 2026-09-07 (portfolio.ts:410-433); x402-il-api 'stays deployed only while it costs ₪0/month, as a RAIL ON STANDBY rather than a line' (portfolio.ts:420-421). 'No human account' contradicted by audits/israel-bureaucracy.md:482-486 (Apify terms gate x402 on KYC; VASP KYC for crypto→ILS). | קווי x402 נהרגו 7.9.2026 (כ-₪6 לספק לחודש). `products/x402-il-api` נשאר מסילה בהמתנה בלבד, כל עוד הוא עולה ₪0. |
| **MEDIUM** | WRONG | 335 | `products/il-biz-tools` - אתר סטטי בעברית עם מחשבונים ומחולל קבלות, מוכן לפריסה ב-Netlify; שער Pro דרך Paddle כשיהיה חשבון. | present | Paddle removed: products/il-biz-tools/src/lib/ has gumroad.js and no paddle.js; src/config/site.json:5-7 gumroad block 'Paste the full https URL of the Gumroad product page here (owner step 3)'; products/README.md 'il-biz-tools moved from Paddle to Gumroad'. §9 (L332-336) also lists 3 products while products/ holds 6 (adds pcn874, mcp-il-tools, telegram-il-tools-bot). | …מוכן לפריסה ב-Netlify; שער Pro דרך Gumroad (צעד 3), מושבת עד שקישור המוצר מודבק. + שלוש השורות החסרות מ-`products/README.md` (pcn874, mcp-il-tools, telegram-il-tools-bot — מושבת). |
| **LOW** | STALE | 3 | עדכון אחרון: 2026-09-02. | present | Sections dated 3.9, 4.9 and 7.9 (L12, L48, L121, L130); last commit touching the file `6db122b` 2026-09-07 (`git log -- docs/INCOME_PLAN.he.md`). | עדכון אחרון: 2026-09-07 (מצב הצעדים: ראה `docs/OWNER_STEPS.he.md`). |
| **LOW** | WRONG | 26 | \| **סה״כ** \| **34,300** \| **4,850–5,850** \| | present | The supervisor column as printed (15,500 + 102,500 + 8,000 + 1,900 + 600 + 500 + 7,800) sums to 136,800. Source: research/colony-sweep/CRITIC-synthesis.md:161 '**136,800** (34,300 excluding the phantom rails column)'. | \| **סה״כ** \| **136,800** (34,300 בלי 102,500 הפנטום של payment-rails) \| **4,850–5,850** \| |
| **LOW** | UNSUPPORTED | 35 | חיתוך של פי 7, עקבי בכל קבוצה ובכל מבקר. | present | The table above (L19-25): agent-markets 600→200 (3×), store-promotion 5.2×, risk-governance 5.4×, bounties-grants 9.75×, israel-bureaucracy 5.3–16×, two groups →0. 7× is the aggregate 34,300/4,850, not per-group. | חיתוך מצטבר של פי 7 (בין פי 3 לפי 16 לקבוצה, ושתיים לאפס). |
| **LOW** | WRONG | 54 | שתי שאילתות המחשבון (מע״מ, שכר נטו) מוחזקות כולן בידי אתרי כלים עצמאיים קטנים | present | research/measurements/serp/2026-09-07-hebrew-calculators.md:332 net-salary top-3 = israelhayom.co.il, taxes-refund.co.il, malam-payroll.com; independents only at #4, #7, #8 ('newspaper + payroll bureau above the independents'). Only the VAT SERP is entirely independents (L201-203). The wording is copied from the file's own summary (L456), which contradicts its table. | שאילתת המע״מ מוחזקת כולה בידי אתרי כלים עצמאיים קטנים, ובשכר נטו שלושה כאלה בעמוד הראשון מתחת לעיתון ולמלם — הוכחה ש… |
| **LOW** | WRONG | 57 | ושמונה משרדי רו״ח | present | research/measurements/serp/2026-09-07-hebrew-calculators.md:303-305 'eight of nine are professional-services firms — six CPA practices (#1, #2, #3, #4, #6, #8) and two law firms (#5, #7)'. | ושמונה משרדי שירותים מקצועיים (שישה רו״ח ושני עורכי דין) |
| **LOW** | STALE | 61 | ושער המפרט עדיין מעליו. | present | The same document L121-128: the official circular was rendered and reconciled that evening; products/pcn874/docs/SPEC.md cites it line by line. | ולידטור חינם קודם; שער המפרט נפתח באותו ערב (ראה למטה). |
| **LOW** | UNSUPPORTED | 72 | זו גם **הראיה היחידה ברמת קוד לזכאות ישראלית בכל הסריקה** | present | src/revenue/rails.ts:340: Gumroad's 'own production source file _13-getting-paid.html.erb carries a row reading Israel \| ILS … This is the strongest payability evidence the 120-criterion sweep produced'; the doc's own L301 relies on it. (portfolio.ts:161 makes the same 'only' claim for Algora — the repo contradicts itself.) | …אחת משתי הראיות ברמת קוד מקור לזכאות ישראלית (השנייה: קובץ המקור של Gumroad, `Israel \| ILS`). |
| **LOW** | STALE | 105 | היעד של ₪20,000 מחייב קווים מקבוצות שעדיין לא נסרקו. | present | All 15 groups swept: docs/REJECTED.md:1021 'content-seo — zero survivors, the fifteenth and last group, audited 2026-09-06'; CHIEF-AUDIT.md:1 'the whole 15-group sweep', result ₪2,200 (L123). Same staleness at L36 ('שש הקבוצות שנותרו'). | כל 15 הקבוצות נסרקו (6.9.2026); התוצאה המבוקרת היא כ-₪2,200, ושום קבוצה לא סגרה את הפער. |
| **LOW** | WRONG | 132 | בשקט — שורה חסרת-תאים, פסיק בסכום שנמחק (`"1800,00"` → ₪180,000), עברית בקבוצת ההפניה שמייצרת רשומה של 62 בייט | present | research/colony-sweep/audits/pcn874-generator.md:8: '3 silently wrong (a row with more cells than columns — an unquoted thousands separator …; a row with fewer cells than columns …; a quoted comma-decimal "1800,00" …), … 1 warned but under-described (Hebrew in refGroup writes a 62-byte record …)'. The doc swaps the extra-cells case for the Hebrew case, which was warned, not silent. | שלושה יצרו קובץ שגוי בשקט — שורה עם יותר תאים מכותרות (מפריד אלפים לא מצוטט), שורה עם פחות תאים, ופסיק עשרוני (`"1800,00"` → ₪180,000); ועברית בקבוצת ההפניה יצרה רשומה של 62 בייט עם אזהרה שלא מסבירה את זה. |
| **LOW** | STALE | 154 | **שש המגבלות שיצאו מהקבוצה הן התוצר האמיתי שלה** | present | src/revenue/constraints.ts PROMOTION_CONSTRAINTS has seven ids (L46, 68, 87, 112, 122, 140, 158); the seventh, no-offsite-funnel-from-a-marketplace-listing (L140), comes from Apify §2.2.4.2(i). The file's header (L5) still says 'six'. | שבע מגבלות נמצאות עכשיו בקוד … ונוספה שביעית: לא לתכנן ליסטינג בשוק כמשפך החוצה (סעיף 2.2.4.2(i) של Apify). |
| **LOW** | UNSUPPORTED | 169 | Apify משלמת $1.4M לחודש ל-~3,000 מפתחים | present | portfolio.ts:280: 'an unverified marketing mean ($470/developer/month across ~3,000 developers, a power-law MEAN and not in Apify's own documentation)'. Arithmetic ($470 ≈ ₪1,730) and the audit's use of it are fine (audits/store-promotion.md:63-65); the figure is stated as fact. | לפי נתון שיווקי שלא אומת (לא בתיעוד של Apify): $1.4M לחודש ל-~3,000 מפתחים… |
| **LOW** | STALE | 171 | \| תוסף WordPress.org עם Pro \| ₪4,000 \| **₪1,000** \| | present | CHIEF-AUDIT.md:136 WordPress '₪0–200 … The ₪1,000 was the earlier audit and did not have the ranking code'; dev-extensions killed (portfolio.ts:447-457). Same for L172 RapidAPI ₪500: CHIEF-AUDIT.md:139 '₪0 forecast; Two auditors disagree (₪500 vs ₪0); the one with 16 months of a real seller's retrospective wins'. | Add a note under the table: (ביקורת ראשית 6.9: WordPress ₪0–200, RapidAPI ₪0; קו התוספים נהרג 7.9). |
| **LOW** | WRONG | 205 | העלות היחידה שלנו שגדלה עם התנועה היא טוקנים. | present | Spans L205-206. research/colony-sweep/CRITIC-synthesis.md:244-251: 'Apify Actors — the deduction before the 80% is Apify platform usage: compute and proxy, not inference … RapidAPI listing — hosting and uptime. Not one surviving line has inference as its dominant marginal cost.' The doc's own L191-193 says this; L222 repeats the error. | טוקנים הם עלות התפעול של המושבה (סריקות), לא של החנויות; עלות השוליים של Apify היא שימוש בפלטפורמה. |
| **LOW** | UNSUPPORTED | 212 | המוצרים שלנו כבר יושבים על תשתית בצורת Cloudflare | present | products/il-biz-tools/netlify.toml (Netlify); apify-il-open-data Dockerfile (Apify); products/x402-il-api/README.md:67 'Deploy on any Node 22.12+ host … Fly, Railway, a VPS'. No product carries Cloudflare/wrangler config. Copied from scouts/bounties-grants--ai-credits-programs.md:249-250. | \| **Cloudflare Workers AI** \| 10,000 neurons ביום \| אפס חסימות בעלים \| |
| **LOW** | UNSUPPORTED | 213 | \| טוקנים של OpenAI תמורת שיתוף נתונים \| ההקצאה הגדולה מהשלוש \| | present | scouts/bounties-grants--ai-credits-programs.md:210-212: 'Usage tiers 1–2: up to 250,000 tokens/day …; tiers 3–5: 1M/day and 10M/day' — below Cerebras's 1M/day at the tiers a new account reaches. | \| טוקנים של OpenAI תמורת שיתוף נתונים \| 250 אלף ביום בדרגות 1–2, עד 10M בדרגה 5 \| **נפסל — ראה למטה** \| |
| **LOW** | STALE | 221 | ובבוט הטלגרם | present | products/telegram-il-tools-bot is PARKED ('none — do not start it', products/README.md table; portfolio.ts:444). | …העשרה בכמות ב-`apify-il-open-data`. |
| **LOW** | UNSUPPORTED | 262 | **Medium Partner** — אוסר כתיבת AI מאחורי ה-paywall ומשלם דרך Stripe. **Substack/beehiiv** — Stripe. | present | docs/REJECTED.md:1347: 'the rejections above that rest on it (Substack, beehiiv, Medium Partner, Polar) are provisional, not closed'; the doc's own L87 says the Stripe limit may not be used to reject a line. | **Substack/beehiiv/Medium Partner** — נשענים על Stripe; פסילה זמנית עד שתיבדק שאלת Stripe-ישראל (צעד 4 סוגר אותה). |
| **LOW** | STALE | 334 | מוכן ל-`apify push` אחרי פתיחת חשבון Apify | present | .github/workflows/apify-publish.yml publishes from CI (push to main, workflow_dispatch, schedule) with the APIFY_TOKEN secret (owner step 6, early half); main is live since 2026-09-22. | מתפרסם בחינם מ-CI (`apify-publish.yml`) ברגע שיש `APIFY_TOKEN` בסודות של GitHub (החצי המוקדם של צעד 6). |
| **LOW** | UNSUPPORTED | 336 | רץ במצב חינמי עד שמוגדר ארנק | present | No deployment recorded anywhere; products/x402-il-api/README.md:67 is a deploy instruction; the board keeps it only 'while it costs ₪0/month' (portfolio.ts:420-421). | …בנוי ונבדק; לא פרוס. מסילה בהמתנה, לא קו. |
| **LOW** | STALE | 341 | **המחקר** נעשה ב-4 סוכני-סריקה במקביל | present | Since 2.9: a 15-group, 121-criterion sweep with supervisors and auditors, a chief audit and a board (research/colony-sweep/CHIEF-AUDIT.md:1; portfolio.ts:161; research/colony-sweep/BOARD.md). | **המחקר**: סריקה של 15 קבוצות (121 קריטריונים) עם מפקח ומבקר לכל קבוצה, ביקורת ראשית ודירקטוריון (3–7.9.2026) — `research/colony-sweep/`. |

## `docs/OWNER_STEPS.he.md` — 156 claims checked, 27 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | STALE | 30 | ## צעד 1 — למזג את PR #2 ב-GitHub (או להגיד לי "תמזג") | changed | Merged on 22.9.2026. `git log origin/main` shows `31cda66 2026-09-22 21:39:15 +0300 Merge pull request #2`. `git log origin/main \| grep -c "colony tick"` gives 16, the latest being a88aa99 on 2026-09-25 13:44:58. The 'done' update to this doc (ad28d27, plus bda1ee0 for the heading) is only on the PR branch: `git branch -a --contains ad28d27` returns only remotes/origin/claude/monthly-income-plan-pfs7vu. Line 43 ('ה-PR פתוח, לא טיוטה'), line 52 ('עד עכשיו הרצתי את זה ידנית'), table row 1 (line 299) and line 3 ('עודכן 7.9.2026') are stale for the same reason. The PDF is the pre-merge one: ad28d27 shows `Bin 244888 -> 291029 bytes`, and this branch still has 244888. logs/CHECKPOINT.md:60 also still says PR #2 is open. | Port ad28d27+bda1ee0. Heading: `## צעד 1 — ✅ בוצע 22.9.2026 · למזג את PR #2 ב-GitHub (או להגיד לי "תמזג")`. This shape keeps owner-steps.test.ts:122 passing. Line 43: '(מוזג ב-22.9.2026, קומיט 31cda66; ה-workflow colony רשום ורץ על main.)'. Line 52: 'מאז 22.9 זה רץ כל שעה בלי אף אחד'. Table row 1: '✅ בוצע 22.9'. Update line 3's date and regenerate the PDF. |
| **HIGH** | WRONG | 113 | **הסתייגות כנה:** ל-Pro של המחשבונים יש עוד תנאי בצד שלי — קובץ שיעורי המס ל-2026 מסומן `verified: false`, ואסור לי לפרסם שיעורים לא מאומתים. זה אצלי, לא אצלך. | present | Pro is document branding on invoice.html (products/il-biz-tools/README.md:14,191-195). invoice.html is gated on vat.json, not tax-2026.json: publish-gate.js:34-35 has `'net-salary.html': ['src/config/tax-2026.json'], 'invoice.html': ['src/config/vat.json']`. Pro is actually gated on gumroad.productUrl and pro.publicKey (src/lib/gumroad.js:50-66; site.json has `pro.publicKey: null`). The repo assigns both to the owner, outside the seven steps: README.md:206 says '**Setting it up (owner, once):** node scripts/make-license.js init'; README.md:252-258 says 'One-time steps only the owner can do … create the Pro product … run make-license.js init'. It also assigns per-sale work to him: scripts/make-license.js:2-6 says 'The owner runs this after a Gumroad sale … issue <buyer> # per sale', and README.md:229 agrees. `grep -rn make-license` finds no automation. logs/CHECKPOINT.md:81 says 'Pro נשאר מושבת עד pro.publicKey ו-gumroad.productUrl (צעד 3 של הבעלים)'. So line 6 ('הם כל מה שצריך כדי שהמכונה תוכל להתחיל למכור') is also contradicted: there is an unlisted owner action and a recurring per-sale owner task. | '**הסתייגות כנה:** Pro של המחשבונים (מיתוג המסמך) דורש עוד שני דברים אחרי צעד 3: מוצר Pro ב-Gumroad ומפתח חתימה לרישיונות (scripts/make-license.js). את שניהם אני עושה, לא אתה, והנפקת מפתח לכל מכירה תהיה אוטומטית — אם זה לא אפשרי, אגיד לך לפני שמשהו נמכר, כי זה יהיה צעד נוסף שלך.' In the same change, fix products/il-biz-tools/README.md or add the step. The two must agree. |
| **HIGH** | WRONG | 129 | 1. פתח https://algora.io ולחץ **Sign in with GitHub** 🔍. | present | The code requires signing in as the brand machine account. src/revenue/owner-steps.ts:154 says 'Done SIGNED IN AS THE BRAND MACHINE ACCOUNT (which is why it now follows step 7)'. src/revenue/portfolio.ts:167 says 'Sign in to Algora AS THE BRAND MACHINE ACCOUNT'. BOARD.md:199 says the same, and so does state/colony/REPORT.md on main. In the doc this appears only 17 lines later, inside the 'התנגשות פתוחה' section, after the step's actions. An owner following 'מה לעשות' in order signs in with whatever GitHub account his browser holds, which is his personal one. That creates the Algora account and the Stripe onboarding under his handle: the byline leak the board reordered the checklist to prevent. | '1. **התנתק מ-GitHub והתחבר כחשבון המכונה של המותג** (זה שפתחת בצעד 7 — ראה (ב) למטה). רק אז פתח https://algora.io ולחץ **Sign in with GitHub** 🔍. אם בחרת במסלול (א) — התחבר בחשבון האישי.' |
| **MEDIUM** | STALE | 47 | בדקתי: ב-`main` היום יש רק שני workflows של בדיקות; הלולאה שלי לא רשומה בכלל. המיזוג מעביר הכול ל-`main`. | present | `git ls-tree --name-only origin/main .github/workflows/` returns 7 files: apify-publish.yml, ci.yml, colony.yml, pcn874-spec-watch.yml, products-ci.yml, release.yml, render-watch.yml. The pre-merge parent 90816df had only ci.yml and release.yml. | Past tense: 'לפני המיזוג היו ב-main רק שני workflows; המיזוג (22.9) העביר הכול, ומאז colony.yml רשום ורץ.' |
| **MEDIUM** | UNSUPPORTED | 63 | הטופס הוא **טופס 6101 (דין וחשבון רב-שנתי)** — פתיחת תיק לעצמאי, מוגש אונליין באתר ביטוח לאומי 🔍 (המספר של הטופס מאומת; מיקומו באתר לא). | present | The only source in the repo is research/colony-sweep/scouts/israel-bureaucracy--business-registration.md:30, tagged `[snippet]`: a search snippet, not a rendered page. The same scout (line 32) could not confirm the neighbouring VAT form number and says 'treat as unknown'. `grep -rn 6101` finds only that scout, logs/2026-09-07-board-owner-guide-and-first-builds.md:26 and this doc. btl.gov.il is egress-blocked (src/revenue/sweep-workflow.ts:41). | 'הטופס הוא כנראה **טופס 6101 (דין וחשבון רב-שנתי)** — המספר הגיע מתקציר חיפוש, לא מעמוד שנפתח; אם באתר ביטוח לאומי הוא נקרא אחרת, חפש "פתיחת תיק עצמאי" 🔍.' |
| **MEDIUM** | WRONG | 156 | בהמשך אבקש ממנו **טוקן אחד** (Personal access token) בשם `BRAND_GITHUB_TOKEN`, כדי שה-PRs ייצאו ממנו — אגיד לך מתי, כשהקוד שצורך אותו יהיה קיים. | present | The doc and the code disagree. owner-steps.ts:141 says 'Its personal access token becomes BRAND_GITHUB_TOKEN in step 6'. owner-steps.ts:165 lists BRAND_GITHUB_TOKEN in step 6's unlocks. BOARD.md:201 puts it in 6b ('Netlify link, GUMROAD_ACCESS_TOKEN, and BRAND_GITHUB_TOKEN'), and BOARD.md:232 asks for a 'tokens table with BRAND_GITHUB_TOKEN'. The doc's step 6 heading (line 187), part B (line 206) and table (line 304) all say 'שני טוקנים'. On current code the doc is literally right: nothing reads the token, and grep finds it only in comments at src/revenue/bounties/intake.ts:15 and index.ts:9. But deferring it costs the owner a second trip to GitHub settings after step 6. Step 6 now comes after step 7, so the machine account already exists by then. | Add a third row to step 6's table: '\| BRAND_GITHUB_TOKEN \| Personal access token של חשבון המכונה (מצעד 7) \| נשמר עכשיו כדי שלא תצטרך לחזור; ישמש כשה-PR הראשון ייצא \|'. Change the headings to 'שלושה טוקנים'. Or, if deferral is kept, change owner-steps.ts:141,165 to match. |
| **MEDIUM** | WRONG | 208 | **טוקן Apify**: פתח https://console.apify.com, הירשם (אימייל; **בלי** אימות זהות — לא צריך כדי לפרסם חינם). | present | The doc leaves out the brand username that the code, the board and the live report all require. owner-steps.ts:170 says 'Apify sign-up with the BRAND as the username (the Store URL apify.com/<username>/… is public)'. portfolio.ts:95 says the same. BOARD.md:201 agrees, and BOARD.md:232 lists 'Apify username' among the P0 amendments to this very document. products/README.md row 1 agrees, and state/colony/REPORT.md on main shows the same text. This is the first action the doc tells the owner to take after step 1. (Also outside this doc: products/apify-il-open-data/docs/PUBLISH.md:27 still says 'Create an Apify account … in your own name'.) | 'הירשם עם **שם המשתמש = שם המותג** (הוא מופיע בכתובת הציבורית apify.com/<שם>/…), אימייל, **בלי** אימות זהות — לא צריך כדי לפרסם חינם.' |
| **MEDIUM** | STALE | 217 | \| `APIFY_TOKEN` \| הטוקן מ-Apify \| עובד ברגע שה-workflow שאני בונה עכשיו יתמזג (היום/מחר) \| | present | .github/workflows/apify-publish.yml is on origin/main (see `git ls-tree origin/main .github/workflows/`). It reads secrets.APIFY_TOKEN (apify-publish.yml:128,220). Its publish job runs on workflow_dispatch or on a push under products/apify-il-open-data/** (apify-publish.yml:33-40,69). The daily schedule only counts runs, so after the paste the agent must dispatch it. | 'עובד מיד — ה-workflow כבר על main; אחרי שתדביק, אני מפעיל את ההעלאה ואומר לך "עכשיו" לקליק בחלק ג.' |
| **MEDIUM** | WRONG | 284 | **אחרי 12 חודשים:** התקרה המבוקרת היא **כ-₪2,200 לחודש** משישה קווים (עד ₪3,500 אם הגבול השנוי במחלוקת של Apify יחזיק). | present | The arithmetic is right (CHIEF-AUDIT.md:123; BOARD.md:127-131). The presentation contradicts the board's ruling on this exact sentence. BOARD.md:257: 'Amended to distinguish the ₪1,500 the portfolio now commits to from the ₪700 that is conditional, so the owner is not told a conditional number as a plan'. portfolio.ts:16-25 and 328-337 say the conditionals 'are therefore not lines'. The portfolio has 4 lines worth ₪1,500. Of the ₪700, ₪400 (Devpost) needs the owner to say yes to per-win paperwork, which defaults to NO (portfolio.ts:365-366; BOARD.md:350-351), and this doc never asks him. | '**אחרי 12 חודשים:** ₪1,500 לחודש מארבעת הקווים — המספר שהתוכנית מתחייבת אליו. עוד ₪700 תלויים בתנאים שעוד לא קרו (₪300 אם דף אגרת הרשם יגיע ל-100 צפיות בשבוע; ₪400 רק אם תסכים לחתום על טפסים בכל זכייה ב-Devpost — ברירת המחדל: לא). עד ₪3,500 אם הגבול השנוי במחלוקת של Apify יחזיק.' |
| **LOW** | UNSUPPORTED | 25 | מה שהם **דורשים** (ת"ז, בנק, טוקן) מאומת מהמקורות שלהם עצמם ב-GitHub. | present | This holds for Gumroad (antiwork/gumroad; scouts/storefronts--gumroad.md:154-164), Algora (algora-io/algora) and Apify (apify-docs). It does not hold for the government sites in the same sentence: form 6101 is `[snippet]` (scouts/israel-bureaucracy--business-registration.md:30). Nor does it hold for the Stripe fields in step 4: scouts/bounties-grants--oss-bounties.md:115-117 has no Stripe source, and stripe.com is blocked (scouts/storefronts--gumroad.md:5). | 'מה ש-Gumroad, Apify ו-Algora **דורשים** מאומת מהמקורות שלהם עצמם ב-GitHub; דרישות האתרים הממשלתיים ו-Stripe — מסיכומי חיפוש בלבד.' |
| **LOW** | WRONG | 46 | כל הקוד שבניתי — המנוע, חמשת המוצרים, המחקר — יושב היום על "ענף" צדדי. | present | `ls products/` lists six product directories: apify-il-open-data, il-biz-tools, mcp-il-tools, pcn874, telegram-il-tools-bot, x402-il-api. The merge commit 31cda66's title says 'six products'. The 'side branch' half is stale (see line 30). | 'כל הקוד שבניתי — המנוע, ששת המוצרים, המחקר — היה על ענף צדדי עד המיזוג ב-22.9.' |
| **LOW** | STALE | 103 | זה מאומת מקוד המקור שלהם עצמם, והוא היחיד שמצאנו עם הוכחה כזו. | present | Freemius was rendered on 7.9 (research/measurements/freemius-rail.md:340-358,376-381; rails.ts:352-353 `evidence: "rendered"`). It pays Israel, and its USD balance can be paid out converted to ILS via Wise or wire. Gumroad remains the only rail with a direct `Israel \| ILS` bank row. | '…והוא היחיד שמצאנו עם תשלום ישיר בשקלים (Freemius משלם מיתרה בדולרים, עם המרה לשקלים דרך Wise או העברה).' |
| **LOW** | WRONG | 108 | **העמלה, כדי שתדע:** 12.9% + $0.80 לכל מכירה, ועוד 2.9% + $0.30 כי מוכר ישראלי לא יכול לחבר Stripe משלו. | present | The processor fee is counted twice. research/colony-sweep/audits/storefronts.md:111-137 derives 12.9% + $0.80 from Gumroad's code as the sum of 10% + $0.50 (Gumroad) and 2.9% + $0.30 (processor, added `if charged_using_gumroad_merchant_account?`). The spec comment there reads `fee_cents eq(209) # 100c (10% flat fee) + 50c + 29c (2.9% cc fee) + 30c (fixed cc fee)`. Lines 148-153 explain that an Israeli seller always pays the rate that INCLUDES the processor fee. The doc's next sentence proves it: '$9 ≈ 22%' equals 12.9% + $0.80 alone ($1.96 = 21.8%, audits/storefronts.md:145). Adding another 2.9% + $0.30 would give 28%. The same error is in src/revenue/rails.ts:81,129 and skills/revenue-il-biz-tools/SKILL.md:21-22. | '**העמלה:** 12.9% + $0.80 לכל מכירה — זה כבר כולל 2.9% + $0.30 של סליקה, שמוכר ישראלי תמיד משלם כי אינו יכול לחבר Stripe משלו. על מוצר של $9 זה בערך 22%; על $19 בערך 17%.' |
| **LOW** | WRONG | 112 | זה פותח **שלושה קווי הכנסה**: מחולל דוח המע"מ (PCN874), שכבת ה-Pro של המחשבונים ב-`il-biz-tools`, ושעון הציות לרשם החברות. | present | owner-steps.ts:116 gives the gumroad step two lines: `lines: ["il-biz-tools", "pcn874"]`. The registrar item is a CONDITIONAL_TARGET (`registrar-reminder`, portfolio.ts:350-360), and portfolio.ts:333-337 says these 'are therefore not lines, carry no budget'. It is not built until the fee page reaches 100 weekly views. The summary table (line 301, '3 קווים') repeats the error. | 'זה פותח **שני קווי הכנסה**: מחולל PCN874 ו-Pro של il-biz-tools. (שעון הציות לרשם החברות הוא אופציה, לא קו — נבנה רק אם דף האגרה מגיע ל-100 צפיות בשבוע.)' Table row 3: '2 קווים, תשלום בשקלים'. |
| **LOW** | UNSUPPORTED | 117 | ההמלצה: לא. שלוש סיבות מאומתות: הם עלולים לדרוש **סרטון סלפי**, האישור לפי שיקול דעת ומוכרים חדשים נדחו, ו**אין תשלום בשקלים**. | present | src/revenue/rails.ts:136-147 grades exactly these three Paddle risks as `evidence: "snippet"`, sourced to CHIEF-AUDIT §3.1/§4A.2. No rendered Paddle page is in the repo. rails.ts:81 calls a different three reasons 'rendered', so the repo contradicts itself. | 'ההמלצה: לא. שלוש סיבות (מסיכומי חיפוש, לא מעמודים שנפתחו): …' |
| **LOW** | UNSUPPORTED | 131 | מלא: **Individual**, שם חוקי, תעודת זהות, כתובת בישראל, חשבון בנק ישראלי (אותיות לטיניות). | present | The Latin-letters rule is sourced for Gumroad only (scouts/storefronts--gumroad.md:162). The Stripe Express requirements (scouts/bounties-grants--oss-bounties.md:115-117) say 'an Israeli bank account in the same legal name' and nothing about script. | Drop '(אותיות לטיניות)' from step 4, or mark it 🔍 as unverified. |
| **LOW** | WRONG | 170 | קנה אותו בכל רשם דומיינים 🔍 | present | BOARD.md:200 says 'a registrar with WHOIS privacy on by default (Cloudflare Registrar, Namecheap)'. owner-steps.ts:128 and portfolio.ts:130 say the same. | 'קנה אותו ברשם שמסתיר את פרטי הבעלים כברירת מחדל (למשל Cloudflare Registrar או Namecheap) 🔍' |
| **LOW** | UNSUPPORTED | 220 | אבל לפי התיעוד של Apify עצמו, **להפוך אותו לציבורי בחנות זה קליק בקונסול**: Actor → **Publication** → **Publish to Store** 🔍. | present | No rendered Apify page in research/ states this. It is asserted without a citation in products/apify-il-open-data/docs/PUBLISH.md:41 and in a workflow comment (apify-publish.yml:180-182). The scouts cite apify-docs publishing/index.mdx (scouts/risk-governance--automation-tos.md:97) but quote nothing about publishing from the console. | '…להפוך אותו לציבורי בחנות זה קליק בקונסול (כך אנחנו מבינים; לא ראינו את המסך): Actor → Publication → Publish to Store 🔍.' |
| **LOW** | WRONG | 238 | ## צעד 7 — להעביר את הריפו לארגון ב-GitHub | present | The doc has drifted from the code. owner-steps.ts:138 titles step 7 'להעביר את הריפו לארגון ב-GitHub (וחשבון מכונה בשם המותג)' with `minutes: [10, 15]`; the doc says '10 דקות' at line 240. owner-steps.ts:162-163 gives step 6 `[15, 20]`; the doc says '15 דקות' at line 189. The table rows at lines 304-305 match the doc, not the code. The doc's step-7 actions (lines 243-251) never create the machine account; it appears only in step 4's option (ב), so the owner has to go back to the org to add it. owner-steps.test.ts:121-139 checks neither titles nor minutes, which is why this passes. | Heading: '## צעד 7 — להעביר את הריפו לארגון ב-GitHub (וחשבון מכונה בשם המותג)', with 'זמן: 10–15 דקות'. Add after item 1: '1א. פתח חשבון GitHub נוסף בשם המותג (אימייל אחר) — חשבון המכונה, ראה צעד 4 (ב) — והוסף אותו כחבר בארגון.' Step 6: 'זמן: 15–20 דקות'. Update table rows 6-7 to match. |
| **LOW** | STALE | 249 | אחרי ההעברה, **בדוק שהסודות מצעד 6 עדיין מופיעים** בהגדרות הריפו (Settings → Secrets). אם לא — הדבק שוב. טוקן Gumroad שנעלם עוצר בשקט את הדבר היחיד שרושם כסף בלדג'ר. | present | This was written for the pre-board order. The ruled order is 1,2,3,5,7,4,6 (doc line 10; owner-steps.ts:60,137,160), so step 7 comes before step 6. At step 7 only the early APIFY_TOKEN can exist; GUMROAD_ACCESS_TOKEN and the Netlify link (line 251) are step-6 items done after the transfer. Line 210's secrets URL under zarfatinimrod-creator/automaton is also wrong for step 6 in that order, because the repo will be in the org by then. | 'אחרי ההעברה, אם כבר הדבקת APIFY_TOKEN — בדוק שהוא עדיין מופיע (Settings → Secrets). את שאר הסודות תדביק בצעד 6, שבא אחרי זה.' Drop line 251's Netlify sentence. Line 210: 'פתח את הריפו בארגון החדש → Settings → Secrets and variables → Actions'. |
| **LOW** | WRONG | 258 | בלי הצעד הזה, אני **לא יכול לפרסם** שום דבר מהריפו בלי שהשם שלך ייצא איתו. | present | This is overbroad. The ruled order publishes the Apify Actor from this repo before step 7 (owner-steps.ts:168-173, earlyPart after step 1), under a brand username. `grep -rn zarfatinimrod products/` finds nothing in any product manifest. The real leak is the repo URL and raw links (skills/README.md:53-58; MISSION.md:296-300). | 'בלי הצעד הזה, כל קישור לקוד עצמו (github.com/… ו-raw.githubusercontent.com/…) נושא את שם המשתמש שלך, ולכן אני לא מפרסם אותו בשום מקום.' |
| **LOW** | WRONG | 266 | **רק לפני שמתמחרים** שם משהו. | present | Apify KYC is triggered by stranger users, not by pricing. portfolio.ts:80 says 'strangerUsers30d at or above 50 → one more Actor …, and Apify KYC goes to the owner'. Pricing is at 200 (portfolio.ts:81). BOARD.md:270-271 and BOARD.md:328 say the same. Line 7's framing ('רק אם קו מסוים מתחיל להרוויח') is similarly loose. | '**רק אם 50 זרים או יותר הריצו את ה-Actor ב-30 יום** (תמחור מתוכנן רק מ-200).' |
| **LOW** | STALE | 271 | **Telegram Stars / USDC**: רק אם יצטבר שם משהו, ואז יש אימות זהות נוסף. לא עכשיו. | present | telegram-bots was killed on 2026-09-07 as a mandate collision. portfolio.ts:434-445: "Fragment's payout KYC is ID plus SELFIE — a camera step the owner's brief forbids". products/README.md marks telegram-il-tools-bot 'PARKED … do not start it'. Nothing can accumulate there, and the step it names is a camera step. | '**USDC**: רק אם יצטבר שם משהו (אין על זה תוכנית). Telegram Stars — סגור: התשלום דורש סלפי.' |
| **LOW** | STALE | 277 | **צד שלי:** הדירקטוריון מחליט (רץ עכשיו). ה-workflow של Apify נבנה עכשיו. מחולל PCN874 מחכה ל**מפרט הרשמי** — הוא חסום לקונטיינר שלי, ואני מוריד אותו דרך GitHub Actions | present | The board ruled on 2026-09-07 (research/colony-sweep/BOARD.md:3). apify-publish.yml is on main. The official PCN874 spec was rendered: research/rendered/pcn874-gov-il-874-eng.meta.json has fetchedAt 2026-09-07T18:28:25Z and status 200. The generator exists (products/pcn874/src/generate.ts), and products/README.md says the validator and generator are built (311 tests). The registrar-fee page (products/il-biz-tools/registrar-fee.html) and the Algora intake filter (src/revenue/bounties/) are also built. | '**צד שלי:** הדירקטוריון פסק ב-7.9. ה-workflow של Apify, מחולל PCN874 (לפי החוזר הרשמי של רשות המסים, שהורד דרך GitHub Actions), דף אגרת רשם החברות ומסנן הבאונטי — בנויים. מה שנשאר (פריסה, חיבור, באונטי ראשון) מחכה לצעדים שלך.' |
| **LOW** | WRONG | 280 | **הערכה שלי, לא מספר מהמחקר:** שבועות בודדים אחרי שכל 7 הצעדים בוצעו, אם באונטי ראשון מאושר (2–5 ימים אחרי אישור — זה כן נמדד). | present | BOARD.md:213-215 rules that this section should say 'first transaction id 4–10 weeks after the checklist, if a stranger finds anything at all'. So a research-backed number exists. The 2–5 days is Algora's own bot text ('100% of the bounty is received 2-5 days post-reward', scouts/bounties-grants--oss-bounties.md:30). It is documented, not measured by us. | '**שקל ראשון בלדג'ר:** 4–10 שבועות אחרי שכל 7 הצעדים בוצעו (פסק הדירקטוריון), ורק אם זר מוצא משהו. באונטי: 2–5 ימים אחרי אישור — כך Algora כותבים, לא מדדנו.' |
| **LOW** | STALE | 290 | שתי זוויות שאף אחד לא בדק: נכס באנגלית במקום בעברית, והפצה דרך GitHub עצמו | present | The English-language angle was occupancy-tested on 7.9 through the EAA wave the board ordered (BOARD.md:288-293) and then closed. Sources: research/measurements/eaa-occupancy.md, docs/REJECTED.md 'The two measurements', and logs/CHECKPOINT.md 'EAA (#22): סגור … ≥11 חינם'. GitHub distribution was ruled YES and is blocked on step 7 (BOARD.md:294-299). | 'שתי זוויות: נכס באנגלית — נבדק ב-7.9 דרך חוק הנגישות האירופי ונסגר (יש כבר ≥11 סורקים חינמיים); הפצה דרך GitHub — מאושרת, ומחכה לצעד 7.' |
| **LOW** | WRONG | 307 | ועוד שתי החלטות שהן שלך: Paddle (המלצה: לא) והמסלול ב-Algora (המלצה: ב'). | present | The doc itself carries a third decision: step 2's accountant / zero-rating choice (lines 82-86). BOARD.md:346-359 lists five owner questions: Devpost paperwork, the bounty handle, Paddle, the reading of ₪200, and Search Console. The Devpost one carries ₪400 of the ₪2,200 in line 284. | 'ועוד החלטות שהן שלך, עם ברירת מחדל שחלה עד שתגיד אחרת: Paddle (לא), המסלול ב-Algora (ב'), רו"ח/אפס-מע"מ (לא), חתימה על טפסים בכל זכייה ב-Devpost (לא), וה-₪200 כסכום חד-פעמי (כן).' |

## `state/colony/REPORT.md` — 52 claims checked, 7 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 51 | ## What the owner has to do (one time, per line) — [per-line lists at lines 53-67, repeated in Blocked on at lines 46-49, followed by 'When a line's steps are done, confirm it'] | changed | Owner step 2 (tax-file: עוסק פטור + ביטוח לאומי) appears nowhere in REPORT.md. src/revenue/owner-steps.ts:95-107 gives it lines [apify-actors, il-biz-tools, oss-bounties, pcn874] and unlocks: 'The legal right to receive any shekel at all ... business income needs a file at the Tax Authority, and ₪10 counts.' The report also never points to docs/OWNER_STEPS.he.md, which MISSION.md:322 names as the one ordered checklist.  How it happened: portfolio.ts:93 took step 2 out of every humanSetup ('it is owner step 2, once'), following BOARD.md:99. renderReport (runner.ts:458-466) prints only humanSetup, so step 2 went from once to zero times.  Computed with ownerStepsForLine() against the steps cited in each humanSetup (scratch tsx script): - apify-actors: gating [1,2,6], cited [6] - il-biz-tools: gating [1,2,3,5,6], cited [3,5,6] - oss-bounties: gating [1,2,7,4,6], cited [7,4] - pcn874: gating [1,2,3,5,7,6], cited [3,7]  Step 1 is already done (31cda66 merged PR #2 on 2026-09-22). Leaving step 2 out for apify-actors is defensible because free publishing takes no money. For il-biz-tools, pcn874 and oss-bounties, which sell through Gumroad or Stripe, it is not. No test checks this: owner-steps.test.ts:94-103 only requires at least one step per line. | Build each line's list in renderReport (src/revenue/runner.ts:458-466) from ownerStepsForLine(line.id). Or print this above the per-line lists: 'Every line that takes money is also blocked on owner step 2 (עוסק פטור file + ביטוח לאומי), listed once. The single ordered checklist is docs/OWNER_STEPS.he.md: 1 → 2 → 3 → 5 → 7 → 4 → 6, and step 1 was done on 22.9.2026.' |
| **HIGH** | WRONG | 54 | Sign up at Apify with the brand as the username ... and paste APIFY_TOKEN as a GitHub Actions secret (owner step 6; this half may be done straight after step 1). Nothing else: publishing free and counting stranger runs needs no identity verification. | changed | The same text also appears in Blocked on (line 46). The CI push creates the Actor PRIVATE, and making it public takes a one-time Console click by the owner. Sources: (1) .github/workflows/apify-publish.yml:180-183: '`apify push` creates or updates the Actor and its build. Making it PUBLIC in the Store is a one-click step in the Console the first time (Publication -> Publish to Store); ... this workflow deliberately does not touch that surface.' (2) products/apify-il-open-data/README.md:141,149-150, under 'this is the whole list': '3. The first push creates the Actor privately. Open it in the Console once and press Publication → Publish to Store to make it visible. One click, once.' (3) docs/OWNER_STEPS.he.md:219-221, step 6 part ג, describes the same click. Consequence: with the token pasted and no click, the Actor stays private and the stranger count reads 0. That trips the kill criterion at src/revenue/portfolio.ts:73: 'strangerUsers30d under 10 at day 30 → instrument only ... permanently unless the count later crosses 50'. | Change humanSetup at src/revenue/portfolio.ts:95 to: 'Sign up at Apify with the brand as the username and paste APIFY_TOKEN as a GitHub Actions secret (owner step 6, early half; step 1 is already done, so this can be done now). After the first CI push, open the Actor in the Apify Console once and press Publication → Publish to Store: the push creates it private, and a private Actor cannot be counted. Neither action needs identity verification.' |
| **MEDIUM** | WRONG | 67 | pcn874: [Gumroad (owner step 3)]; [GitHub organisation (owner step 7)] — and oss-bounties (lines 62-63): [machine account (owner step 7)]; [Algora/Stripe (owner step 4, done after step 7)] — presented as each line's complete setup | changed | pcn874 is also blocked on two steps the report omits: - step 5, domain (owner-steps.ts:129 lines [il-biz-tools, pcn874]) - step 6, ci-tokens: Netlify plus GUMROAD_ACCESS_TOKEN (owner-steps.ts:166). Without that token no Gumroad sale reaches the ledger (colony.yml:53-57; connectors/gumroad.ts:14), and pcn874 sells through Gumroad (portfolio.ts:181).  oss-bounties is also blocked on step 6 (owner-steps.ts:166), whose unlocks text says 'BRAND_GITHUB_TOKEN lets bounty PRs leave the brand account' (:165). src/revenue/bounties/intake.ts:13-16: 'the line is blocked on owner steps 7 ... and 4 ... and on `BRAND_GITHUB_TOKEN`, and a loop that attempted a bounty before those exist would publish a pull request under the owner's handle.' BOARD.md:201 puts BRAND_GITHUB_TOKEN in step 6b.  Line 69 tells the owner to confirm setup-done once the listed steps are done, which would release both lines too early. | Add to pcn874's humanSetup (portfolio.ts:197-200): 'Buy the domain (owner step 5) and paste GUMROAD_ACCESS_TOKEN as an Actions secret (owner step 6), both shared with il-biz-tools.' Add to oss-bounties' humanSetup (portfolio.ts:165-168): 'Paste the brand machine account's token as BRAND_GITHUB_TOKEN (owner step 6; Claude will ask when the code that uses it exists). Do not confirm this line before then.' Alternatively, the generator change proposed in the owner step 2 finding fixes both lines. |
| **MEDIUM** | WRONG | 69 | When a line's steps are done, confirm it so the colony can start building: pnpm exec tsx scripts/colony.ts setup-done apify-actors --evidence "done on <date>" | present | (1) The colony loop cannot build. setup-done only sets the line to proposed and queues a build goal (scripts/colony.ts:305-307). The scheduled loop runs `tick --no-feed` (colony.yml:61), so the board review records 'goal filing disabled for this review (no executor attached)' (heartbeat.ts:351-355) and never files the goal. runner.ts:9-11: 'Director and worker EXECUTION still needs inference.' colony.yml:59-60: 'Governance runs; execution waits.' Building is done by Claude sessions, and has happened already without any confirmation: products/pcn874, products/il-biz-tools and products/apify-il-open-data exist while all four lines are awaiting_setup.  (2) The owner is told elsewhere to report back rather than run a command. docs/OWNER_STEPS.he.md:224 says 'תגיד לי "צעד 6 בוצע..."', the summary table has a 'מה לשלוח לי' column (:297), and logs/CHECKPOINT.md:683 has the agent run setup-done. Run on the owner's machine, the command writes only to his local state/colony/colony.db (default --db, colony.ts:49). The loop reads the copy committed on main, which colony-bot re-commits after every tick. | Replace runner.ts:467-471 with: 'When a line's steps are done, tell Claude which steps and when. Claude records it (pnpm exec tsx scripts/colony.ts setup-done <line> --evidence "...") and commits the state. That releases the line from awaiting_setup and queues its build goal. The scheduled loop does not build; a Claude session does.' |
| **LOW** | WRONG | 54 | Apify KYC and a PayPal payout are deferred until stranger runs exist. | present | Also on line 46. The repo's threshold is 50 stranger users in 30 days, not 'stranger runs exist': - portfolio.ts:80: 'strangerUsers30d at or above 50 → one more Actor ... and Apify KYC goes to the owner' - BOARD.md:328: 'Second Apify Actor / Apify KYC / Apify pricing \| 50 / 50 / 200 stranger users' - Under 10 at day 30, KYC is never asked for (portfolio.ts:73). The payout rail is 'PayPal or Wise' (portfolio.ts:282), not PayPal only. | 'Apify KYC and a PayPal or Wise payout are deferred until 50 stranger users in 30 days. If fewer than 10 by day 30, they are never asked for.' |
| **LOW** | UNSUPPORTED | 67 | Create the GitHub organisation under the brand name so the open-source core and the npm scope carry it and not your username (owner step 7) | present | Also on line 49. Nothing in the repo creates or owns an npm scope: - `grep -rn NPM_TOKEN` over the repo (node_modules excluded) returns nothing. - No workflow publishes to npm. - No owner step covers an npm account. - products/pcn874/package.json is "name": "pcn874", "private": true. The only mentions are the plan text itself (portfolio.ts:180,199,324; BOARD.md:72). No repo source says the GitHub org (step 7) gives the brand an npm scope. If a separate npm account is needed, it would be an eighth owner step, which owner-steps.test.ts forbids. | 'Create the GitHub organisation under the brand name so the open-source core carries it and not your username (owner step 7).' Drop the npm scope from the owner's text until the repo shows how it is created and by whom. |
| **LOW** | WRONG | 77 | Money here is only what reached the ledger with a platform transaction id. | present | This holds for sale, subscription, payout and refund (ledger.ts:408-414). A cost may be recorded without an id: ledger.ts:412 says 'Only 'cost' may omit it', and scripts/colony.ts:99-101,265 allows it from the CLI. The Costs (30d) and Net (30d) rows on this page can therefore include hand-entered money with no platform id. The effect today is nil because there are no cost rows. | Change runner.ts:477 to: 'Revenue here is only what reached the ledger with a platform transaction id; costs may be entered by hand without one. Projections are never counted.' |

## `products/README.md` — 101 claims checked, 15 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 7 | step 6 — Apify sign-up with the brand username + `APIFY_TOKEN` (allowed straight after step 1; **no KYC**) | present | Every other repo source that covers publishing says the token alone leaves the Actor private and a Console click is needed: products/apify-il-open-data/README.md:149-150 ('The first push creates the Actor privately. Open it in the Console once and press Publication → Publish to Store'); .github/workflows/apify-publish.yml:180-181; products/apify-il-open-data/docs/PUBLISH.md:41; docs/OWNER_STEPS.he.md:219-222 (step 6 part C). logs/CHECKPOINT.md:285 records that an earlier audit found this click missing from the owner guide; it was added there but not here, and not to owner-steps.ts:170-171. If the Actor stays private the stranger count reads 0, and portfolio.ts:73 turns 'strangerUsers30d under 10 at day 30' into a permanent 'instrument only' verdict. Caveat: sibling audit owner-steps.md F23 notes that no rendered Apify page backs the click itself. | step 6 — Apify sign-up with the brand username + `APIFY_TOKEN` (allowed straight after step 1; **no KYC**), then one click after the first CI push: Apify Console → Actor → Publication → **Publish to Store** (without it the Actor stays private and the stranger count reads zero) |
| **MEDIUM** | WRONG | 3 | Each directory is standalone (own package.json, tests, README with deploy steps and the owner's one-time setup). | present | products/mcp-il-tools/README.md has no deploy section and no owner section (headings: Install, Tools, On 1-800, What this is honest about, The paid version, Development), and at :53 says the logic 'is sold per-call over the x402 protocol', which contradicts line 12. products/il-biz-tools/README.md:252-271 'One-time steps only the owner can do' lists owner work that is not among owner-steps.ts's seven: Search Console verification (:262), 'confirm the 2026 brackets … flip verified … Nobody here may flip it' (:266-267), and registrar amounts (:268-270). portfolio.ts:109 makes the rate check agent work, and MISSION rule 1 says never invent a step. products/telegram-il-tools-bot/README.md:7 'What the owner does once (no KYC)' plus BotFather contradicts this index's line 11 ('KYC needs a selfie … do not start it'). | Each directory is standalone (own package.json and tests; most READMEs also carry deploy steps). The owner's steps are the seven in docs/OWNER_STEPS.he.md and nothing else — where a product README lists an owner step that is not there (il-biz-tools: Search Console, tax-rate verification; telegram-il-tools-bot: BotFather), the checklist wins. |
| **MEDIUM** | WRONG | 7 | Apify Store, **published free** while the 30-day stranger count runs | present | The Actor has not been published and no count is running. state/colony/REPORT.md:32 (same on origin/main): `apify-actors \| core \| awaiting_setup`. logs/CHECKPOINT.md:60-61: none of the owner's seven steps has been done. state/colony/measurements/apify-runs.json, the file the daily count writes, does not exist. apify-publish.yml exits 0 with a notice while APIFY_TOKEN is missing. The phrase is the line's planned name (portfolio.ts:37), but here it appears in bold, with no tense, as the rail's state. | Apify Store — **to be published free** (not yet: awaiting step 6 and the Publish-to-Store click); the 30-day stranger count starts when it goes public |
| **MEDIUM** | WRONG | 9 | \| `pcn874` \| pcn874 \| **Gumroad** (ILS) — validator and generator built, no price set \| step 3 — Gumroad account + token \| | present | src/revenue/owner-steps.ts, the source line 14 names for this table, lists pcn874 under step 5 domain (:123-129), step 7 github-org (:136-142) and step 6 ci-tokens (:160-166), as well as steps 1-3. portfolio.ts:198-199 (pcn874 humanSetup) names step 3 AND step 7 ('Create the GitHub organisation under the brand name so the open-source core and the npm scope carry it'). The il-biz-tools row shows its line-specific steps 3/5/6; pcn874 has 3/5/6 plus 7 but shows only 3. products/pcn874/README.md:5 has the same omission. | step 3 — Gumroad account + token; step 5 — domain; step 7 — GitHub organisation (open-source core and npm scope); step 6 — GUMROAD_ACCESS_TOKEN into GitHub secrets |
| **MEDIUM** | UNSUPPORTED | 28 | any USDC that arrives is booked through `src/revenue/connectors/x402-local.ts` | present | x402-local.ts:6,23,35 imports only runtime `transactions` rows of type transfer_in/credit_purchase whose description carries `[line:<id>]`; untagged rows are skipped. The only insertTransaction calls are src/agent/tools.ts:327-333 (credit_purchase 'x402 credit topup: $…', no line tag) and :1031 and :1769 (transfer_out). `grep -rn -i 'ledger\|transactions\|sqlite\|automaton' products/x402-il-api/src` finds nothing: USDC settles on-chain to X402_PAY_TO and nothing reads it back. products/x402-il-api/scripts/tag-payment.md:33 says 'The runtime records the inbound transfer in transactions with the tag', but no code does this. Its fallback is a manual `scripts/colony.ts record … --source x402` (:43-45), which bypasses x402-local.ts and tags the now-killed [line:paid-apis]. | any USDC that arrives has to be recorded by hand (scripts/colony.ts record … --source x402 --external-id <tx hash>, per products/x402-il-api/scripts/tag-payment.md); x402-local.ts only books transactions rows tagged [line:<id>], and nothing writes such a row for an inbound USDC settlement yet |
| **MEDIUM** | WRONG | 55 | `.github/workflows/pcn874-spec-watch.yml` watches all three hashes so a new edition is noticed rather than assumed away. | present | products/pcn874/docs/SPEC-SOURCES.lock.json:3 is `"sources": {}`, the same at HEAD and origin/main. scripts/spec-watch.mjs:125-128: a URL with no stored entry is printed as 'new' and never sets `changed`, so only a stored hash that differs gives exit 1. The workflow has `permissions: contents: read` (pcn874-spec-watch.yml:30-31) and no commit step, so the lock written at spec-watch.mjs:148 is thrown away with the runner. Every run therefore reads an empty lock, reports all three as new, and exits 0. tests/spec-watch.test.ts:39-41 asserts `expect(lock.sources).toEqual({})` ('this job, which has never run in CI'). The only partial signal is render-watch.yml's weekly silent '[skip ci]' commit of a changed PDF (render-watch.yml:49,128). | `.github/workflows/pcn874-spec-watch.yml` downloads all three documents weekly but cannot yet flag a new edition: its lock file holds no hashes and the job cannot commit one, so every run passes. Until the three SHA-256s in research/rendered/pcn874-*.meta.json are committed to docs/SPEC-SOURCES.lock.json (and tests/spec-watch.test.ts stops requiring it empty), the only signal is a silent 'render: … changed' commit from render-watch.yml. |
| **LOW** | WRONG | 8 | step 6 — Netlify link | present | owner-steps.ts:160-167 (step 6) covers the Netlify link AND pasting GUMROAD_ACCESS_TOKEN ('lets the loop read sales and write each one to the ledger'). portfolio.ts:131 gives il-biz-tools' step 6 as 'Link the repo in Netlify and paste GUMROAD_ACCESS_TOKEN as a GitHub Actions secret'. | step 6 — Netlify link + GUMROAD_ACCESS_TOKEN into GitHub secrets |
| **LOW** | STALE | 30 | Gumroad is the only merchant of record with rendered proof of ILS payout to an Israeli bank. | present | Freemius was rendered on the same day (7.9.2026): src/revenue/rails.ts:347-354 evidence 'rendered'. It is a merchant of record (research/rendered/freemius-allowed-prohibited.txt:240 'As your Merchant of Record') and pays Israel (freemius-supported-countries.txt:308 'Israel'). freemius-your-earnings.txt:143,146-150: ILS sales go into the USD balance, and 'For international Wise and wire transfers … you can convert payouts to your local currency'. Gumroad remains the only one with a direct 'Israel \| ILS' row. This agrees with sibling audit owner-steps.md F22. | Gumroad is the only merchant of record with a rendered direct ILS payout row for Israel; Freemius (rendered 7.9.2026, rails.ts CANDIDATE_RAILS) pays Israel from a USD balance, convertible to ILS only via Wise or wire. |
| **LOW** | WRONG | 37 | Every rule in `products/pcn874/docs/SPEC.md` cites that document by line | present | SPEC.md:299 'There is no rule in the validator today whose basis is oss-only except the line-ending…'. SPEC.md:359-361 says the same, and src/validate.ts:968-971 cites only accounter: and rcbuilder: sources for file.lineEnding.mixed. | Every rule in products/pcn874/docs/SPEC.md but one cites that document by line; the exception, the file.lineEnding.mixed warning, rests on the open-source implementations and says so. |
| **LOW** | WRONG | 38 | **Six of the seven recorded disagreements are resolved** (five by the document, one by a newer vendor manual) | present | The numbers contradict themselves: six resolved plus the two open questions named in the same sentence is 8, not 7. The seven (git show 0761326^:products/pcn874/README.md:32) are SPEC §6.1, 6.2, 6.4, 6.5, 6.6, 6.7, 6.8. In SPEC.md headings, §6.1/6.4/6.5/6.8 are 'RESOLVED (official)', §6.2 is settled by the H-ERP manual, and §6.6 and §6.7 are 'STILL OPEN'. So five of the seven are resolved (four by the document, one by the manual) and two are open. 'Five by the document' is only reached by counting §6.3, which was not one of the seven. The likely origin is SPEC.md:424-428 ('Six were settled by the document; one by a vendor manual; one is still open'), which contradicts its own §6.6/§6.7 headings. | Of the seven recorded disagreements, five are resolved (four by the document, one by a newer vendor manual) and two stay open — the reportedVat arithmetic and the line-ending/empty-file question. (The eighth numbered place, zeroOrExemptSalesAmount, was also settled by the document.) |
| **LOW** | WRONG | 40 | the `reportedVat` arithmetic and the line-ending/empty-file questions **stay open, and no rule was invented for them** | present | reportedVat has no rule (correct). The line-ending and empty-file questions do have warning rules: `file.lineEnding.mixed` (src/validate.ts:959-977, sources accounter and rcbuilder only) and `file.detail.none` (:1106-1112). SPEC.md:521 says 'warn on mixed, warn on a detail-free file', and SPEC.md:359-361 says the line-ending warning 'is the one rule in the product whose basis is oss-only'. | …stay open: no reportedVat rule exists at all, and the line-ending/empty-file questions carry warnings only (file.lineEnding.mixed, which rests on open-source code alone, and file.detail.none). |
| **LOW** | UNSUPPORTED | 52 | there is now a second test that reads the cited lines and requires the finding's quote to come from them. | present | products/pcn874/tests/validate.test.ts:703-709 and :727-737 require only that the quote and the cited lines share at least one run of four consecutive words (RUN = 4, shared.length > 0). Its own comment at :692-695 says 'It cannot prove the rule follows from the document, but it does catch a quote that has drifted'. products/pcn874/README.md:68 describes it correctly ('share four consecutive words'). | …a second test that reads the cited lines and requires the finding's quote to share a run of four consecutive words with them. |
| **LOW** | WRONG | 60 | it builds the text, runs `validatePcn874` on it, and returns nothing at all if that reports an error | present | On refusal, generatePcn874 still returns a result with `text: null`, the validator's findings and counts, and parsed.records emptied (src/generate.ts:1180, 1205; docs/GENERATOR.md:4-8). The CLI prints the findings and writes no file. | …and returns no file text at all — only the findings — if that reports an error |
| **LOW** | WRONG | 74 | **Products with no line:** `mcp-il-tools`, which is a distribution channel test rather than a storefront | present | The document's own table calls x402-il-api a 'standby rail, not a line' (line 12), and says the telegram-il-tools-bot line is killed (line 11; KILLED_LINES, portfolio.ts:435). Three products have no live line, not one. | **Products with no live line:** mcp-il-tools, a distribution channel test rather than a storefront (registry listing blocked as below), plus the parked telegram-il-tools-bot and the standby x402-il-api described above. |
| **LOW** | UNSUPPORTED | 75 | whose registry listing is blocked on the domain (step 5) and the organisation (step 7). | present | This is backed by BOARD.md:243, but it is presented as the complete blocker set. The rendered registry quickstart the repo cites (research/colony-sweep/scouts/distribution--partnerships-integrations.md:13-16) puts `npm publish` with `mcpName` in package.json before `mcp-publisher publish`. But products/mcp-il-tools/package.json has no mcpName (grep exit 1), no NPM_TOKEN or npm-account step exists anywhere in the repo, and the .github/workflows/mcp-publish.yml that BOARD.md:243 lists does not exist. | …whose registry listing is blocked on the domain (step 5) and the organisation (step 7), and on publishing @bediyuk/mcp-il-tools to npm — for which no step, token or workflow exists yet. |

## `products/apify-il-open-data/docs/PUBLISH.md` — 56 claims checked, 16 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 27 | **Create an Apify account** at `apify.com` in your own name. | present | src/revenue/portfolio.ts:95 (the line's humanSetup, also in main's colony.db): "Sign up at Apify with the brand as the username — the Store URL apify.com/<username>/… is public". src/revenue/owner-steps.ts:170 says the same. docs/OWNER_STEPS.he.md:22: "השם הציבורי יהיה שם מותג, לא השם שלך". MISSION.md:274-279: nothing published carries the owner's name or username. scripts/apify-runs.mjs:310 and :332 build actorId `${me.username}~${actorName}` and storeUrl `https://apify.com/${me.username}/${actorName}`, so the username is the public namespace. | Create an Apify account at console.apify.com, email and password. The legal identity is yours, but the USERNAME must be the brand: the Store URL is apify.com/<username>/… and it is public. No identity documents at this stage. |
| **HIGH** | STALE | 29 | **Install the CLI and log in**, from a machine with the repo checked out: npm install -g apify-cli / apify login ... **Push the Actor** from the product directory: cd products/apify-il-open-data / apify push ... **Send back the Store URL.** That is the whole handover. | present | products/apify-il-open-data/README.md:164 "Deploying is not a manual step any more. .github/workflows/apify-publish.yml does it, and the only thing it needs is one repository secret named exactly APIFY_TOKEN". README.md:188 labels the CLI route "Deploy steps by hand (fallback)". apify-publish.yml:150-162 runs `apify-cli@1.10.0 login --token`, and :184-192 runs `push --wait-for-finish=900`. docs/OWNER_STEPS.he.md:206-222 (step 6) is: create a token, paste APIFY_TOKEN, one Console click. src/revenue/owner-steps.ts:165 "Converts every 'the owner must push' recurring operation into a one-time step." logs/CHECKPOINT.md:294 "owner pastes APIFY_TOKEN + click Publish to Store". MISSION.md:322-323 says never invent a step. PUBLISH.md never mentions APIFY_TOKEN, and without it apify-runs.mjs:294-301 exits 0 and writes nothing, so the 30-day count never starts. `npm install -g apify-cli` is unpinned, while the workflow pins 1.10.0 because its verified behaviours are version-specific (apify-publish.yml:164-169). | Replace 'What the owner does' with the OWNER_STEPS step 6 path: (1) create an Apify account, email and password, with the BRAND as the username (the Store URL apify.com/<username>/israel-open-data-api is public), no identity documents; (2) create a Personal API token and paste it as the GitHub repository secret APIFY_TOKEN (never in chat); (3) tell me 'done', and I run the apify-publish workflow (pasting a secret triggers nothing by itself) and tell you when the push finished; (4) in the Console, Actor -> Publication -> Publish to Store (🔍 menu names unverified), leave it free, no KYC, then tell me 'published'. The CLI route (npx apify-cli@1.10.0 login --token ... && npx apify-cli@1.10.0 push) is a fallback only; see the README section 'Deploy steps by hand (fallback)'. |
| **HIGH** | WRONG | 43 | **Tell the colony it is live**, so the loop stops treating it as blocked: pnpm exec tsx scripts/colony.ts setup-done apify-actors --evidence "published free to Apify Store on <date>, URL <url>" | present | scripts/colony.ts:70 "setup-done <lineId>  Mark a line's one-time owner setup as done and queue its build goal." At :302-311, setHumanSetupDone(true), and if the status is awaiting_setup it becomes "proposed" (:306), not live, then enqueueGoal({phase:"build"}). MISSION.md:329-332: a line becomes live only when money lands. The loop runs on GitHub, not locally: colony.yml:23 hourly cron, and :80-99 `git add -f state/colony` pushes the binary colony.db. Main's colony.db still shows apify-actors awaiting_setup with human_setup_done 0 (query of a `git show origin/main:state/colony/colony.db` copy). The doc gives no commit or push step, so the local change never reaches the loop; a pushed binary DB would also race the hourly bot commit. The command needs a root `pnpm install` (tsx, native better-sqlite3), which is unstated. OWNER_STEPS.he.md:224 has the owner report 'done' in chat instead. | Replace step 5 with: 'Tell me it is published. I run `setup-done apify-actors --evidence "<your message, date>"` in the loop's own state on main. That moves the line from awaiting_setup to proposed; it becomes live only when money lands in the ledger.' |
| **HIGH** | WRONG | 49 | What happens then, without you ... Apify's Console shows runs and unique users per Actor. | changed | The only automated count is the count-runs job (apify-publish.yml:198-221), and it needs APIFY_TOKEN (apify-runs.mjs:294-301). This doc never asks for the token. Without it, only the Console can be read, and research/colony-sweep/audits/agent-markets.md:161-172 says apify.com and api.apify.com are egress-blocked from the colony: "Either the owner reads the dashboard (a recurring manual op MISSION forbids)". Even with the token nothing feeds the result into the colony: portfolio.ts:44 promises "a daily stats job writes strangerRuns30d and strangerUsers30d through recordKpi", but no code outside portfolio.ts references either KPI, and src/revenue/connectors/apify-stats.ts, planned at CHECKPOINT.md:264, does not exist (`ls src/revenue/connectors`). The Console sub-claim is snippet-grade only (agent-markets.md:216-219). | Once APIFY_TOKEN is set, a GitHub Actions job (apify-publish.yml, count-runs, daily 05:41 UTC) writes state/colony/measurements/apify-runs.json to main, and you read nothing. (Open gap: the colony does not yet read that file into its KPIs, so the day-30 decision is taken by hand from that file.) |
| **HIGH** | UNSUPPORTED | 51 | For 30 days the only thing that matters is the run count from people we did not tell. | present | This depends on scripts/apify-runs.mjs:92-99 and :245-269: GET /v2/actors/{username~name}/runs with the OWNER's token is assumed to return runs other accounts started, split by run.userId. Nothing in the repo shows this. apify-runs.mjs:240-243: "This container cannot reach api.apify.com at all … neither path could be exercised against the real service". logs/2026-09-07-apify-publish-and-measure.md:22-23 checked only the response shape via Context7, :110 ran the stranger split against a mocked API, and :117-119 says the real API response was not verified. The only rendered Apify data, research/rendered/apify-store-accessibility.json (api.apify.com/v2/store, fetched from a runner), exposes public per-Actor aggregates (stats.totalUsers30Days, stats.publicActorRunStats30Days) that the script does not use. If the runs list is caller-scoped, strangerRunsLast30Days is 0 by construction and is committed as a 'real zero' (apify-runs.mjs:34-35), the false zero the script's header (:26-32) says it prevents. | Do not present the stranger run count as established. State it as an assumption in apify-runs.mjs and README.md:183. Also record the public Store aggregate (GET /v2/store?search=israel-open-data-api, then stats.totalUsers30Days minus the brand account), which the repo has rendered evidence for, until the first real response shows which number sees strangers. |
| **MEDIUM** | WRONG | 13 | Publishing it costs no build hours, no money, and no owner time beyond account creation | present | The same document then lists six owner steps (lines 27-47): account, CLI install and login, push, Console publish, colony command, and sending the URL. Even the CI path needs a token, a pasted secret and one Console click (OWNER_STEPS.he.md:206-222; README.md:142-150). | Publishing it costs no build hours and no money, and about five minutes of owner time: an account, one token pasted as a GitHub secret, and one click in the Apify Console (docs/OWNER_STEPS.he.md step 6). |
| **MEDIUM** | STALE | 15 | ₪3,000 (`apify-actors` target) | present | src/revenue/portfolio.ts:84-87: "Board §3: RETARGET ₪3,000 → ₪200", targetMonthlyAgorot: agorotFromIls(200). TARGET_BASIS portfolio.ts:274: "There is no argument for ₪3,000 the board was willing to sign". Main's colony.db: apify-actors target_monthly_agorot 20000 (₪200). docs/REJECTED.md:1214 `apify-actors \| ₪3,000 \| **₪200**`. | ₪200 (the apify-actors target, retargeted from ₪3,000 by the board), ₪1,500 (store-promotion audit, kept as the contested upper bound) and ₪200 (agent-markets audit) |
| **MEDIUM** | WRONG | 27 | (apify-publish.yml:27-29) WHAT THE OWNER HAS TO DO: paste one repository secret named exactly APIFY_TOKEN. | changed | Pasting the secret triggers nothing. The publish job runs only on a push to main touching products/apify-il-open-data/** or the workflow file, or on workflow_dispatch (apify-publish.yml:33-39; `if: github.event_name != 'schedule'` at :69). The daily schedule runs only count-runs, which on an unpushed Actor takes the 404 path (apify-runs.mjs:316-322): a notice, exit 0, nothing written, every day behind a green check. origin/main (PR #2 merged 2026-09-22, 31cda66) has no state/colony/measurements yet. src/revenue/owner-steps.ts:170 "This alone starts the 30-day stranger count" is contradicted. No document names who dispatches; OWNER_STEPS.he.md:222 implies the agent. | WHAT THE OWNER HAS TO DO: paste APIFY_TOKEN (OWNER_STEPS step 6). Adding the secret does not trigger this workflow: after it is set, run it once by hand (Actions -> apify-publish -> Run workflow). The agent does this. Then make the Actor public with one Console click. |
| **MEDIUM** | STALE | 51 | For 30 days the only thing that matters is the run count from people we did not tell. | present | research/colony-sweep/BOARD.md:267-273 (§6.3.1): "Readout rule: strangerUsers30d = distinct users minus the brand account. Under 10 stranger users at day 30…". BOARD.md:328 sets triggers at 50/50/200 stranger USERS. portfolio.ts:73, :80-81 put the kill and scale criteria on strangerUsers30d. apify-runs.mjs:130-200 (summariseRuns) outputs runsLast30Days, strangerRunsLast30Days and byStarter, all run counts, and computes no distinct-user count. The workflow commit subject and summary (apify-publish.yml:244, :272-279) report runs only. | For 30 days the only thing that matters is how many distinct people who are not us run it. The board reads strangerUsers30d (BOARD.md §6.3.1), not the run count. |
| **MEDIUM** | STALE | 54 | **If strangers run it:** constraint 7 has its first real answer, KYC becomes worth doing, and pricing gets switched on with the table already written into the README. | present | BOARD.md:267-273 and :328, and portfolio.ts:73, :80-81: under 10 stranger users at day 30 the line stays an instrument with no KYC request; 10-49 keep counting; 50+ one more Actor and KYC is put to the owner; 200+ pricing is DESIGNED, and the listing must state that the source is free at data.gov.il. Pricing changes also need 14 days' notice and are limited to once a month (research/colony-sweep/scouts/risk-governance--automation-tos.md:95). The README table does exist (README.md:116-119). | If 50 or more strangers run it in 30 days, Apify KYC is put to you as the next step. At 200 or more, pricing is designed, and the listing must say the source is free at data.gov.il. From 10 to 49 we keep counting; under 10 the line stays a measuring instrument (BOARD.md §6.3.1). |
| **MEDIUM** | WRONG | 66 | The listing says the Actor is free and that pricing is planned but not enabled. It does not promise a price, because the listing cannot carry one yet | present | The listing is README.md (actor.json:13 "readme": "../README.md"), the actor.json description and the input schema. README.md:108: "The table below is the pricing that will be turned on later", with $0.005 / $0.002 (:116-119) and "Tip: 1,000 companies for $2" (:123). .actor/INPUT_SCHEMA.json:40 and README.md:37 say "You are charged only for what is returned" / "You are charged only for returned items", present-tense on a free Actor. actor.json:5 says pricing is "configured but not enabled", but nothing is configured on Apify; ppeEvents is documentation only (apify-publish.yml:88-93). | The listing says the Actor is free, and shows a suggested future price table that is not enabled. Two strings still read as if users are charged today and should be fixed before publishing: INPUT_SCHEMA.json maxRecords ('You are charged only for what is returned') and the actor.json description ('pricing is configured'). |
| **LOW** | WRONG | 16 | ₪500 and ₪200 (the two audits above) | present | 'The two audits above' are productized-services and agent-markets (PUBLISH.md:6-7). Agent-markets gives ₪200 (agent-markets.md:43, :460). The productized-services audit gives ₪0 net-new, and ₪150–250 at 12 months only if four gates clear (productized-services.md:68, :296-311, :333). The ₪500 is that group's supervisor headline, which the same audit refutes: productized-services.md:314 "## 2. The group headline ("₪500/month") — REFUTED as a portfolio number". | ₪0 net-new (productized-services audit; ₪150–250 only if four gates clear) and ₪200 (agent-markets audit) |
| **LOW** | WRONG | 24 | none of it is reversible in a way that matters. | present | Read literally, this says nothing can be undone, the opposite of the intended reassurance. The one step whose effect is sticky and public is the choice of username, which becomes the Store namespace (apify-runs.mjs:332; portfolio.ts:95). | …and nothing here is hard to undo, except the Apify username, which becomes part of the public Store URL. Choose the brand name. |
| **LOW** | UNSUPPORTED | 41 | **Publish it to the Store** from the Apify Console: Actor → Publication → *Publish to Store*. Leave the pricing model as **Free**. | present | The repo's own checklist marks these menu names unverified: OWNER_STEPS.he.md:24-26 (🔍: menu names and click order not verified) and :221 ("Publish to Store 🔍"). The only Apify file in research/rendered/ is a /v2/store search result. README.md:150 and apify-publish.yml:180-182 repeat the path without a source. Nothing shows a 'pricing model' selector in the publish flow. | Add: '🔍 menu names are not verified from here. Look for the function (publish to Store), not the exact words.' |
| **LOW** | STALE | 57 | which would kill several ₪-thousand ceilings in this repo that currently rest on the opposite assumption. | present | The board has already cut them. The committed portfolio is apify-actors ₪200, il-biz-tools ₪400, oss-bounties ₪300 and pcn874 ₪600, ₪1,500 in total (portfolio.ts:87, TARGET_BASIS; CHECKPOINT.md:242, :272; main colony.db). The only ₪-thousand Apify figure left is the ₪1,500 contested upper bound (portfolio.ts:278). | …it settles the ₪1,500 contested upper bound the board refused to commit to (TARGET_BASIS['apify-actors']), and it tests whether platform search delivers a first user at all (MISSION constraint 7). |
| **LOW** | WRONG | 96 | (apify-publish.yml:95-97) What this catches: `pricingModel`, `pricingInfos`, `pricePerUnitUsd`, `pricingPerEvent` or any future key with "pric" in its name being added to the manifest. | changed | The guard regex '"[A-Za-z0-9_]*[Pp]ric[A-Za-z0-9_]*"[[:space:]]*:' is case-sensitive apart from the first P, and excludes '-'. Running it on {"PRICE_USD":1,"unit-price":2,"pricingModel":"X"} matches only "pricingModel":. Against the real actor.json: FOUND=["suggestedPriceUsd"] UNEXPECTED=[], so it passes. The named examples are all caught. | …or any future camelCase or snake_case key containing 'pric'/'Pric' (upper-case and hyphenated keys are not caught). Or use grep -i and allow '-' in the character class. |

## `products/il-biz-tools/README.md` — 96 claims checked, 25 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 229 | After a sale the owner runs `make-license.js issue <sale id>` and puts the resulting key where Gumroad delivers it to the buyer — the product's content / licence field. | present | This is a recurring per-sale owner task. MISSION.md:318-327 (rule 1) puts every owner step in the one checklist and says the owner does not talk to customers. docs/OWNER_STEPS.he.md step 3 (:94-100) ends at minting the access token, and src/revenue/owner-steps.ts:109-120 has no issuance. Repo research shows it can be automated: scouts/storefronts--gumroad.md:111-119 (API v2 sales index/show, licenses verify/rotate, webhooks; 'A software agent can create, price, publish, discount and fulfil products'), and OWNER_STEPS.he.md:216 says the loop reads Gumroad sales hourly once GUMROAD_ACCESS_TOKEN is set. The key is minted after the sale with the sale id as subject (make-license.js:59), but product content is fixed per product and shared by all buyers, so a per-sale key cannot be delivered that way. 'confirm it at owner step 3': step 3 has no such check. | Licence delivery is an open design gap, and it is not an owner step. Keys must be issued without the owner, for example by a CI job that reads new sales with GUMROAD_ACCESS_TOKEN and signs them with a private key held as a GitHub secret, or by switching to Gumroad-issued licence keys. Until that exists, Pro should not go on sale even once the URL and public key are set. |
| **HIGH** | STALE | 241 | Push the repo. In Netlify: *Add new site → Import from Git*, pick the repo. | present | logs/2026-09-03-first-products.md:18,23: the owner's Netlify account exists and the project il-biz-tools (il-biz-tools.netlify.app, site id 2087c2ed-5270-4407-8746-675d6ea41d5e) was created. logs/CHECKPOINT.md:220-221: get-project returns il-biz-tools with an empty current deploy. docs/OWNER_STEPS.he.md:195-196 (step 6): 'יש כבר אתר בשם il-biz-tools … Link repository'; Add new site is only the fallback. A second site cannot take il-biz-tools.netlify.app, which siteUrl, the 7 canonical tags and sitemap.xml all point at. | 1. In Netlify, open the existing site `il-biz-tools` (id 2087c2ed-…) → build & deploy settings → *Link repository* → `automaton`, branch `main`. Use *Add new site → Import an existing project* only if that site is gone (owner step 6, docs/OWNER_STEPS.he.md). |
| **HIGH** | WRONG | 248 | CLI alternative: `npx netlify-cli deploy --dir=products/il-biz-tools --prod` | present | --dir points at the SOURCE folder. It holds the real net-salary.html (figures from unverified tax-2026.json), README.md, package.json, tests/, scripts/ (make-license.js) and the unfiltered sitemap.xml:6 that still lists net-salary.html. The gate runs only in scripts/build-site.js, which writes _site (build-site.js:30,60-84); a deploy of the source dir skips it. netlify.toml:1-2 and build-site.js:4-6 give the reason _site exists: publishing '.' uploaded package.json, README.md, tests/ and scripts/. Line 242 of the same README says the publish dir is _site. BOARD.md:73 orders pages that depend on tax-2026.json kept unpublished. | CLI alternative (from products/il-biz-tools): `node scripts/build-site.js && npx netlify-cli deploy --dir=_site --prod` (needs NETLIFY_AUTH_TOKEN + NETLIFY_SITE_ID). Never point --dir at the product folder itself: that skips the unverified-rate gate and uploads the dev files. |
| **HIGH** | WRONG | 256 | create the Pro product, then paste its full product URL into `gumroad.productUrl` and run `make-license.js init` | present | Canonical owner step 3 (docs/OWNER_STEPS.he.md:94-100) is: sign up under the brand name, payout settings, ID verification, access token. It has no product creation, no config edit and no make-license.js. MISSION.md:322-323: one checklist, 'Never invent a step that isn't required.' Product creation is agent-doable with the token (storefronts--gumroad.md:111-116, full CRUD on products). Pasting into src/config/site.json is a repo edit. init needs a local clone, Node>=22 (package.json:17) and a commit ('commit that one', make-license.js:43). The same issue appears at README:206 ('Setting it up (owner, once)') and in the Hebrew owner list at 307-308. | 1. **Gumroad** (owner step 3 in docs/OWNER_STEPS.he.md): open the store under the brand name, complete identity and payout details, and mint the access token, exactly as that step says. Creating the Pro product, filling gumroad.productUrl, and generating the licence keypair are agent-side (see the licence-delivery gap under The Pro tier). Until the URL and the public key both exist, the Pro box stays on בקרוב. |
| **HIGH** | WRONG | 266 | **Before `net-salary.html` can be published at all:** confirm the 2026 brackets and NI rates against the Tax Authority booklet and flip `verified` in `tax-2026.json`. Nobody here may flip it. | present | docs/OWNER_STEPS.he.md:112-114 tells the owner: 'קובץ שיעורי המס ל-2026 מסומן verified: false, ואסור לי לפרסם שיעורים לא מאומתים. זה אצלי, לא אצלך.' The board's order, research/colony-sweep/BOARD.md:73 (same at :317 and src/revenue/portfolio.ts:109): pages stay unpublished 'until its rates are confirmed against two independent GitHub-hosted implementations and verified flips to true'. That is an agent task with a different standard (no booklet, no owner). `grep -rn "Nobody here may"` finds only this README line. | Remove it from the owner list. Under The unverified-rate gate: 'net-salary.html is published once tax-2026.json's brackets and NI rates are confirmed against two independent GitHub-hosted implementations (board order, BOARD.md §4 build #4), and then verified is set to true. That is agent work, not an owner step.' |
| **MEDIUM** | UNSUPPORTED | 30 | Gumroad's rules allow a downloadable/licence product, which is exactly what Pro is: a file-free licence key that unlocks branding in the buyer's own browser. | present | The only rendered reading of Gumroad's rules in the repo (research/colony-sweep/scouts/storefronts--gumroad.md:81-103, prohibited.html.erb 'Last revised: August 2, 2026') prohibits 'Services fulfilled outside Gumroad' and 'products with no content attached that direct buyers to contact you on external platforms'. It concludes 'Gumroad only works for self-contained files/courses/memberships delivered on Gumroad'. The only licence pattern it describes is 'a downloadable tool behind Gumroad-issued license keys' (:118-119). Nothing shows a file-free key for a feature on an external site is allowed. | Whether Gumroad accepts a file-free licence key for a feature on an external site is unverified. The rendered prohibited-products page allows self-contained products delivered on Gumroad and prohibits content-less products and services fulfilled elsewhere. Check this before listing, for example by attaching a real file (a branded template pack) with the key. |
| **MEDIUM** | UNSUPPORTED | 39 | \| Osek patur ceiling 2026 \| ₪122,833 (`src/config/osek-patur.json`) \| verified \| | present | The value matches osek-patur.json:3. The 'verified' label is contradicted by docs/OWNER_STEPS.he.md:73-74: '(הנתון ל-2026 כפי שמופיע במקורות שלנו; אף עמוד ממשלתי לא נפתח מכאן, אז תאמת אותו במסך של רשות המסים)'. The sources are secondary: kolzchut (osek-patur.json:6, egress-blocked per groups/israel-bureaucracy.md:45), ynet for VAT (vat.json:5), and a gov.il department home page plus CPA firms for allocation thresholds (allocation-number.json:6-10). groups/israel-bureaucracy.md:51-55 says every legal number, including CONFIRMED ones, rests on snippets. The same README treats six CPA circulars as not enough for the registrar amounts. publish-gate.js:42-44 publishes on the flag. Applies to lines 38 and 45 too. | verified (secondary sources only; no primary government page was opened; see docs/OWNER_STEPS.he.md step 2). Or re-grade VAT, osek-patur and allocation to the gate's standard and record how each was checked. |
| **MEDIUM** | WRONG | 56 | It is not, so the rule is now enforced by the build. | present | build-site.js:32 copies src/config whole (DIRS = ['assets','src/lib','src/config']). A scratch build ships _site/src/config/tax-2026.json (all 7 unverified brackets) and _site/src/config/registrar-fee.json ('reducedIls': 1338, 'fullIls': 1777). assets/page-registrar-fee.js:7 fetches 'src/config/registrar-fee.json' on every visit, and netlify.toml:26-29 sets a cache header for /src/*. The unverified figures are publicly reachable by URL even though no page renders them. | The build enforces it for rendered pages: a page that renders an unverified figure is not published. The config files themselves still ship under /src/config/ (the registrar page fetches its JSON at runtime), so the unverified amounts and brackets are reachable by URL, though no page shows them. Or change the build to stop copying unverified configs that no published page needs. |
| **MEDIUM** | WRONG | 155 | `siteUrl` \| Canonical origin, used for `<link rel=canonical>`; also edit `sitemap.xml` and `robots.txt` | present | The static <link rel="canonical"> is hard-coded to https://il-biz-tools.netlify.app/... in all 7 pages (e.g. vat.html:8), and so is the JSON-LD url (index.html:20). siteUrl only overwrites the canonical at runtime (assets/common.js:17-21). | Canonical origin. JS overwrites the canonical at runtime, but the static <link rel=canonical> in all 7 pages and the url in index.html's JSON-LD are hard-coded, so change those too, along with sitemap.xml and robots.txt. |
| **MEDIUM** | WRONG | 244 | Deploy. Then set the custom domain and update `siteUrl` in `src/config/site.json`, `sitemap.xml` and `robots.txt` to the real domain; commit. | present | `grep -n netlify.app *.html`: the canonical is hard-coded at allocation.html:8, index.html:8, invoice.html:8, net-salary.html:8, osek-patur.html:8, registrar-fee.html:8, vat.html:8, and the JSON-LD url at index.html:20. siteUrl only rewrites the canonical at runtime in JS (assets/common.js:17-21). README:155 has the same gap. OWNER_STEPS.he.md:171 (step 5) tells the owner 'אל תגדיר כלום אחרי הקנייה … ואני אשלח 2–3 שורות (רשומות DNS)', so the file edits and the commit are agent work. | After the owner buys the domain (owner step 5), an agent sends the DNS records and replaces il-biz-tools.netlify.app everywhere: site.json, the 7 canonical tags, the index.html JSON-LD url, sitemap.xml, robots.txt. |
| **MEDIUM** | STALE | 261 | **Netlify** account + domain purchase (or use the free `*.netlify.app` subdomain). | present | The Netlify account and the il-biz-tools site already exist (logs/2026-09-03-first-products.md:18,23). The remaining Netlify action is Link repository in owner step 6 (OWNER_STEPS.he.md:187-204). The domain is owner step 5 with the board's ruling, .com plus WHOIS privacy (OWNER_STEPS.he.md:162-171; owner-steps.ts:128). The free subdomain is not an equivalent: owner-steps.ts:128 says without a domain 'every site URL stays *.netlify.app, and no line that depends on search exists', and BOARD.md:100 lists 'deploy, domain, one SERP read' as preconditions. | 2. **Domain** (owner step 5): buy one .com with WHOIS privacy and configure nothing; the agent sends the DNS records. **Netlify** (owner step 6): link the repo to the existing il-biz-tools site. The account already exists. The free *.netlify.app URL is not a substitute: the SEO line needs the domain. |
| **MEDIUM** | WRONG | 262 | **Google Search Console** verification for the domain. | present | research/colony-sweep/CHIEF-AUDIT.md:295-298 lists it under 'Removed from catalogues … invented, unverified': 'Google Search Console verification (not a legal identity step; agent-doable by committing a file)'. BOARD.md:278-284 (§6.3): GSC is optional, the export is 'asked for only after il-biz-tools shows 100 weekly views. Not a checklist step.' BOARD.md:358-359 says the same. docs/OWNER_STEPS.he.md has no GSC step. Deploy step 4 (line 246) and the Hebrew list (307-308) repeat it. | Drop it from the owner list. Deploy step 4: 'Search Console is optional and deferred (board §6.3): only if the owner chooses to add the property, and only after the site shows 100 weekly views. It is not on the checklist.' |
| **MEDIUM** | WRONG | 263 | Optional: Plausible, or PostHog (`posthog.projectKey` + the cookieless server-hash-mode toggle). | present | BOARD.md:73 ('page views from the PostHog connector attached to this session') and BOARD.md:280-282 ('page views now, from the PostHog connector attached to this session … written weekly as KPIs') make page-view measurement agent-side and required: the 100-weekly-views gate depends on it (BOARD.md:75,341). OWNER_STEPS.he.md has no PostHog step. Line 169's 'no account was created' is at odds with a connector being attached (not provable from the repo alone). | PostHog page views are agent-side (board §6.3: the PostHog connector attached to the session). Plausible stays optional. Neither is an owner step. Line 169: 'posthog.projectKey is empty in the repo; the board expects the key from the PostHog connector attached to the session.' |
| **MEDIUM** | WRONG | 268 | **Before any shekel amount appears on `registrar-fee.html`:** open the primary source (תקנות החברות (אגרות) / רשות התאגידים), correct the amounts in `registrar-fee.json`, and set both `verified` and `renderAmounts` to true. | present | Listed as 'only the owner can do', but it is not in docs/OWNER_STEPS.he.md or src/revenue/owner-steps.ts. The cited research says 'must first have a human or unblocked agent open the primary source' (research/colony-sweep/groups/israel-bureaucracy.md:54-55). The repo has an agent path to gov.il: research/rendered/pcn874-gov-il-874-eng.meta.json shows a gov.il PDF fetched by render-watch.yml with status 200. | Before any shekel amount appears on registrar-fee.html, an agent renders the primary source through render-watch (add the URL to research/rendered/urls.txt with its citation), corrects the amounts and sets both flags. Not an owner step. |
| **LOW** | WRONG | 4 | No framework, no dependencies at runtime: the build only copies files | present | scripts/build-site.js:67-71 generates a withheld-notice page in place of net-salary.html, and :81-84 writes a filtered sitemap. The Hebrew line 285 ('ה-build רק מעתיק קבצים') says the same. | No framework, no runtime dependencies: the build copies an allowlist into _site/, swaps withheld pages for a notice, and filters the sitemap. |
| **LOW** | UNSUPPORTED | 18 | Audience: the ~600k Israeli self-employed | present | `grep -rn -E "600[,.]?000\|600k"` across the repo finds only this README line; no research file backs the figure. | Audience: Israeli self-employed, especially עוסקים פטורים… (drop the number, or cite a source). |
| **LOW** | UNSUPPORTED | 25 | **Pro (document branding): one-time ₪79** through **Gumroad** | present | No price is configured in site.json or any product file. ₪79 appears only in research that quotes the Paddle-era README (scouts/content-seo--converter-utility-sites.md:26; audits/distribution.md:37 records the ₪79 tier as 'REFUTED — it is not purchasable'). | Pro (document branding): suggested one-time ₪79 through Gumroad (not configured anywhere; set when the product is created). |
| **LOW** | UNSUPPORTED | 26 | Gumroad is the merchant of record, sells in ILS (the one payment rail this repo has rendered evidence for), and needs no liveness video to open | present | The rendered evidence is for ILS PAYOUT to an Israeli bank (src/revenue/owner-steps.ts:115, Gumroad's _13-getting-paid.html.erb 'Israel \| ILS'), not for the currency buyers are charged in. The liveness claim rests only on absence: CHIEF-AUDIT.md:248 'No liveness video is reported anywhere'; OWNER_STEPS.he.md:98 'לא מדווח על סלפי-וידאו בשום מקור'. | Gumroad is the merchant of record, pays out in ILS to an Israeli bank (the one payment rail with rendered evidence of that), and no source reports a liveness video in its onboarding. |
| **LOW** | WRONG | 28 | Stripe is not available to an Israeli individual | present | CHIEF-AUDIT.md:200-201: Stripe/Israel has three answers on disk, merchant NO and Connect Express YES at code grade via Algora. docs/OWNER_STEPS.he.md:124-132: owner step 4 is Stripe Connect Express onboarding as an Israeli individual. | Stripe as a direct merchant account is not available to an Israeli individual (Connect Express via Algora is owner step 4). |
| **LOW** | UNSUPPORTED | 54 | MISSION rule 4 — never publish an unverified legal figure — used to be kept by hand | present | MISSION.md:341-345 (rule 4, 'Honest value only') does not contain this wording. The order not to publish unverified rates is the board's: BOARD.md:73,317. | MISSION rule 4 (honest value), as applied by the board (BOARD.md §4 build #4): a page whose rates are unverified is not published. |
| **LOW** | WRONG | 69 | `node scripts/check-html.js` prints the same verdict, and **fails** if an HTML page is missing from the map | present | check-html.js:7 hard-codes 7 pages and checks only [...pages,'404.html'] against the map (:57-59). On a scratch copy with an added foo.html missing from PAGE_RATE_SOURCES: `node scripts/check-html.js` exit 0 ('all pages ok'); `node scripts/build-site.js` exit 1 ('refusing to build: foo.html not listed in PAGE_RATE_SOURCES'). The build and tests/publish-gate.test.js:71-74 catch it; check-html does not. README:129 ('on every page') has the same gap: 404.html gets no title/description/class checks. | node scripts/check-html.js prints the same verdict for its fixed list of 7 pages plus 404.html. The build (build-site.js) and tests/publish-gate.test.js are what refuse an HTML page missing from the map. |
| **LOW** | WRONG | 91 | and the same research file states that not one primary Israeli legal or government source was ever opened — "enough to decide where to build, not enough to publish to users as guidance" | present | The six-circular corroboration is in research/colony-sweep/audits/israel-bureaucracy.md:212-215. The 'not one primary source' sentence is in a different file, research/colony-sweep/groups/israel-bureaucracy.md:51-54, which reads 'That is enough to *decide where to build*; it is **not** enough to *publish to users as guidance*' and says 'rendered by any of the nine agents in this group', not 'ever opened'. | …across six independent accountancy circulars (audits/israel-bureaucracy.md §2.2), and the group report (groups/israel-bureaucracy.md) states that not one primary source was rendered by any agent in that group: 'That is enough to decide where to build; it is not enough to publish to users as guidance.' |
| **LOW** | WRONG | 214 | **Losing `.license-key.json` invalidates every key already issued.** Back it up. | present | Verification uses only the public key in site.json (license.js:61-85). The private file is read only by `issue` (make-license.js:52-57). Losing it stops new issuance. Existing keys stop verifying only when `init` is re-run, which overwrites pro.publicKey (make-license.js:38-40). That is still the only way to resume selling, so the backup advice holds. | Losing .license-key.json means no new key can ever be issued. Re-running init to recover replaces the public key, and every key already issued stops working. Back it up. |
| **LOW** | WRONG | 242 | Publish directory: `_site` (what `netlify.toml` declares: an explicit allowlist, so `package.json`, `README.md`, `tests/` and `scripts/` are never uploaded). (`netlify.toml` already declares this; headers/CSP/redirects are in the same file). | present | netlify.toml:4-6 declares only publish = "_site" and command, with no base. The allowlist is in scripts/build-site.js:32-35 (DIRS, FILES, the .html pages). The base directory has to be set in the UI. The 'never uploaded' part is correct for the Git build: the scratch _site has none of those files. | …Publish directory: _site, written by build-site.js from an explicit allowlist, so package.json, README.md, tests/ and scripts/ are never uploaded. netlify.toml declares the build command, the publish directory, headers/CSP and redirects. The base directory is set in the Netlify UI. |
| **LOW** | WRONG | 259 | Fallbacks if Gumroad refuses: PayPal Business "buy now" link or Payoneer Checkout — swap the one `openProCheckout` call in `assets/page-invoice.js`. | present | There is exactly one call (page-invoice.js:251). But the click handler is attached only when proButtonState(site).enabled (page-invoice.js:243-256), which needs a valid https gumroad.productUrl and a public key (gumroad.js:49-74). The 'ready' note says 'התשלום מתבצע ב-Gumroad' (gumroad.js:80), and page-invoice.js:261 tells buyers the key comes 'ב-Gumroad'. Swapping the call alone leaves the button disabled. | Fallbacks if Gumroad refuses: PayPal Business or Payoneer Checkout. Changing rails means proButtonState/openProCheckout in src/lib/gumroad.js, the config key, and the Gumroad wording in gumroad.js and page-invoice.js, not one call. |

## `products/pcn874/README.md` — 134 claims checked, 18 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 117 | `.github/workflows/pcn874-spec-watch.yml` watches all three hashes so a new edition is noticed rather than assumed away. | present | The lock has never held a hash: products/pcn874/docs/SPEC-SOURCES.lock.json has "sources": {}, on HEAD and on origin/main. tests/spec-watch.test.ts:38-41 pins it empty (`expect(lock.sources).toEqual({})`). scripts/spec-watch.mjs:123-125 prints `new` and exits 0 when a source has no stored hash, and only a stored hash that differs sets changed=true and exits 1 (:126-130, :160-167). .github/workflows/pcn874-spec-watch.yml:30-31 has `permissions: contents: read` and no commit or push step; it uploads only .spec-downloads/, not the lock. So every CI run starts from the empty lock, reports three `new` sources and exits 0, and the CHANGED branch can never run. The weekly render-watch.yml does re-fetch the same URLs (research/rendered/urls.txt:93-95), but it exits 0 on a change (scripts/render-watch.mjs:35), commits with [skip ci] (render-watch.yml:128) and does not regenerate the .txt the tests read (meta.json "textPath": null). A new edition would land as a silent commit. | No automated check fails on a new edition today. pcn874-spec-watch.yml downloads the three documents, but its lock file (docs/SPEC-SOURCES.lock.json) has never held a hash and the job cannot commit one, so it cannot detect a change. The weekly render-watch.yml re-fetches the same three URLs and would commit a changed PDF under research/rendered/, but it does not fail or alert and it does not refresh the extracted text. Noticing a new edition currently depends on someone reading that commit. (Or fix the workflow first and keep the sentence.) |
| **MEDIUM** | WRONG | 5 | Owner one-time step: **step 3** of [`docs/OWNER_STEPS.he.md`](../../docs/OWNER_STEPS.he.md). | present | src/revenue/owner-steps.ts lists "pcn874" in the lines of six steps: merge-pr (:92), tax-file (:103), gumroad (:116), domain (:129), github-org (:142) and ci-tokens (:166). src/__tests__/revenue/owner-steps.test.ts:110-111 asserts ownerStepsForLine("pcn874") equals ["merge-pr","tax-file","gumroad","domain","github-org","ci-tokens"]. The line's own humanSetup (src/revenue/portfolio.ts:196-199) and state/colony/REPORT.md:49 both name step 3 AND step 7. | Owner one-time steps: this line is gated by steps 1, 2, 3, 5, 6 and 7 of docs/OWNER_STEPS.he.md (every step except 4, the Algora/Stripe step). Step 3 (the Gumroad account) is the one specific to it; src/revenue/owner-steps.ts is the source of truth. |
| **MEDIUM** | UNSUPPORTED | 42 | 311 tests cover every rule the validator emits and every path through the generator | present | 311 is right (vitest run: 6 files, 311 passed). Coverage has never been measured: package.json devDependencies are only @types/node, typescript and vitest, vitest.config.ts has no coverage block, and no @vitest/coverage-* is installed. The validator emits `footer.licensedDealerId.digits` (valid-minimal.txt with last line `X51445728A` gives `footer.licensedDealerId.digits:error`), yet no test names it (grep count 0 in tests/*.test.ts) and no fixture triggers it (I ran the validator over all 26 fixtures and collected the rule ids). Twelve generator problem codes are named in no test: csv.column.duplicate, csv.noHeaderRow, meta.generationDate, meta.licensedDealerId, meta.reportMonth, meta.reportedVat.roundingTie, row.counterpartyVatId.digits, row.inputKind.onSale, row.inputKind.unknown, row.refGroup.tooLong, row.refNumber.digits, totals.exportInZeroOrExempt. | 311 tests, in tests/, over 26 generated fixed-width fixtures and 35 generated CSV inputs, including one file per input the generator's own refutation audit constructed. Coverage is not measured. At least one validator rule (footer.licensedDealerId.digits) and twelve generator problem codes are asserted by no test. |
| **LOW** | WRONG | 15 | Every rule cites a line of `research/rendered/pcn874-gov-il-874-eng.txt`. | present | file.lineEnding.mixed (src/validate.ts:959-980) cites only ['accounter:packages/pcn874-generator/src/utils/builders.ts:37,41', 'rcbuilder:...PCN874Manager.cs:571-575'] (:968-971) and no ita: line, so its basis is oss-only. The README itself says so at lines 124 and 188. Every other rule does carry an ita: citation (checked). | Every rule but one cites a line of research/rendered/pcn874-gov-il-874-eng.txt. The exception is the mixed-line-endings warning, which rests on the implementations alone and says so. |
| **LOW** | UNSUPPORTED | 35 | **The reference group is `A(4)` — letters are legal**, in either case \| `ita:…:139` and `ita:…:580-581` | present | ita:139 gives `A(4)` and ita:580-581 gives "internal characters of the submitter". Neither says anything about case. README line 119 says "The document never says which characters an A(n) field admits — not case", and src/validate.ts:78-84 says the same. Accepting lowercase is the product's reading of the document's silence (pcn874-reconciliation.md:102: "nothing restricts case"). The row sits in a table headed "where the Authority says it". | The reference group is A(4), and letters are legal. The document is silent on case, so this validator accepts both. |
| **LOW** | UNSUPPORTED | 36 | `R`'s reference number is zeros | present | Appendix C's R row cell is `Zeros` (ita:467), but the same row carries comment marker D (ita:476), and note D says "The reference number will include the invoice number" (ita:563). README line 123 calls this "ambiguous in the document itself", and src/validate.ts:730-750 keeps detail.R.refNumberZeros a warning for that reason. The row presents it as settled, in the table of what is "backed by the Tax Authority's own document". | ... R's reference-number cell is zeros (but note D, marked on the same row, says the opposite; see below) ... |
| **LOW** | WRONG | 48 | **All thirteen resolutions below held on their primary quote.** | present | It is true that 13 resolutions held (research/colony-sweep/audits/pcn874-reconciliation.md:17: SPEC §6.1-§6.10, §5.1, §5.4, §5.5 — 13 CONFIRMED). But "below" points to the wrong place: the table directly below (lines 53-63) is nine rule changes, and the later table (lines 102-111) has eight rows. Neither lists thirteen resolutions. | All thirteen resolutions in docs/SPEC.md (§6.1-§6.10, §5.1, §5.4, §5.5) held on their primary quote. |
| **LOW** | WRONG | 87 | Six more findings were implemented with it | present | The sentence then lists seven items: (1) the library no longer returns refused records, (2) a stale --out is named, (3) file.byteWidth, (4) the two silent reclassifications (row.vatSum.empty, row.vat.roundedToZero), (5) the all-empty row is dropped, (6) meta.malformed, (7) an empty --reported-vat is a usage error. That is eight if (4) counts as two. The audit numbers them as findings 3-10 (research/colony-sweep/audits/pcn874-generator.md:99-107). | Seven more findings were implemented with it: (or "Eight", counting the two reclassification warnings separately) |
| **LOW** | WRONG | 100 | ## The seven disagreements, resolved / The old spec logged eight numbered places where the implementations disagreed. [`docs/SPEC.md` §6](docs/SPEC.md) settles each and keeps the log as history | changed | The table under the heading (lines 104-111) has eight rows, and two say STILL OPEN (6.6 at line 109, 6.7 at line 110; docs/SPEC.md:516, 521). "Resolved" and "settles each" contradict it. The count of eight is itself correct (git show a65a5b2:products/pcn874/docs/SPEC-FROM-SOURCES.md has §5.1-§5.8). | Heading: "The eight logged disagreements: six settled, two still open". Line 100: "... docs/SPEC.md §6 takes each in turn and keeps the log as history. Six are settled; 6.6 and 6.7 remain open." |
| **LOW** | UNSUPPORTED | 125 | **The Authority publishes a free simulator; use it:** `http://www.misim.gov.il/EmDvhmfrt/wUploadFileHeshboniotSim.aspx` | present | The URL comes from a vendor manual, research/rendered/pcn874-h-erp-mirror.txt:1263, and src/sources.ts:184-189 records it that way. The circular only says a simulator will come "In the nearest time" (ita:84-85). No rendered source says it is free. The same manual says the simulator checks the file only partially, and that transmission can show errors it did not catch (herp:1269-1270). Nothing in the repo shows the http:// URL resolves today (checking would need a network call, which is out of scope). | The Tax Authority runs an online simulator. Its address, as the H-ERP vendor manual gives it (herp:1263), is http://www.misim.gov.il/EmDvhmfrt/wUploadFileHeshboniotSim.aspx. Use it, but note the same manual says it checks the file only partially (herp:1269-1270). |
| **LOW** | WRONG | 149 | without it, `generate` refuses. | present | src/cli.ts:30-33 (USAGE) says --reported-vat "Overrides a \"# reportedVat:\" line in the CSV", and src/generate.ts:1050 reads `options.reportedVat ?? meta.get('reportedVat')?.value`. I ran `node dist/cli.js generate tests/fixtures/csv/minimal.csv --out <scratch>/out.txt` with no flag: exit 0, file written, because minimal.csv:6 has `# reportedVat: 1800`. The README's own synopsis at line 144 shows the flag as optional. | ... without it, and without a `# reportedVat:` line in the CSV, `generate` refuses. The flag overrides the CSV line. |
| **LOW** | WRONG | 198 | It is now "how the Authority states it", which moved five rules: the sign of zero, the two record counts, and the `L` and `Y` constraints became errors; the reference-group check stopped rejecting letters. | present | The sentence lists six items. Sign-of-zero did not move because it did not exist: git show a65a5b2:products/pcn874/src/validate.ts has no signOfZero rule, and README line 211 lists it as "new". The sentence omits detail.K.vatIdZeros, which went from warning (a65a5b2 validate.ts:299-302, 327-333) to error at the reconciliation (git show 0761326:.../validate.ts:459-460; now validate.ts:708-709). | ... which promoted five rules from warning to error (the two record counts and the L, K and Y zeros constraints), added the sign-of-zero rule, and stopped the reference-group check rejecting letters. |
| **LOW** | WRONG | 208 | `Finding.officialText` \| the Authority's own words for the rule | present | file.encoding.ascii sets officialText 'Appendix A types every field as A(n), N(n) or "+/-"; the file has no declared encoding beyond that.' (src/validate.ts:918-919). That is a paraphrase written here. I ran the README's own four-consecutive-word check (logic of tests/validate.test.ts:697-743) over the officialText of every rule the fixtures emit, warnings included, and this is the one that shares no four-word run with its cited lines (ita:96-161, 139, 580-581). The CLI prints it under the label 'Tax Authority:' (src/cli.ts:63). The test only checks errors. | the Authority's own words for the rule where it has words for it (checked against the cited lines for every error; one warning, file.encoding.ascii, carries a paraphrase) — or change file.encoding.ascii's officialText to a verbatim quote. |
| **LOW** | STALE | 233 | eight sample inputs, including the two that must be refused (`no-reported-vat.csv`, `input-no-supplier.csv`) | present | `ls tests/fixtures/csv` shows 35 files: 8 worked examples plus 27 audit-*.csv (added in 32928ae). I ran all 35 through the built CLI and 17 exit 1 (refused): the two named here plus 15 audit files (c1, c2, c3, e2, e3, f, g, h2, h3, j1, l, m, n, n2, p). The same README says 35 at line 42, and docs/GENERATOR.md:262-265 already says "35 generated sample inputs. Eight are the worked examples ... The other 27 are audit-*.csv". | 35 sample inputs: eight worked examples (two of them must be refused, no-reported-vat.csv and input-no-supplier.csv) and 27 audit-*.csv, one per input the generator's refutation audit built. 15 of those must be refused, and tests/generate.test.ts names each one's expected outcome. minimal.csv is built to reproduce valid-minimal.txt byte for byte, and a test asserts it. |
| **LOW** | WRONG | 250 | כל כלל מצטט שורה מתוך `research/rendered/pcn874-gov-il-874-eng.txt`. | present | Same as line 15: file.lineEnding.mixed (src/validate.ts:968-971) cites no ita: line (basis oss-only), as README lines 124 and 188 admit. | כל כלל, פרט לאחד, מצטט שורה מתוך research/rendered/pcn874-gov-il-874-eng.txt — החריג הוא האזהרה על סיומי שורה מעורבים, שנשענת על המימושים בלבד. |
| **LOW** | WRONG | 252 | **שבע המחלוקות שנרשמו קודם הוכרעו**: חמש לפי המסמך, אחת לפי מדריך ספק חדש יותר, ואחת — החישוב של הסכום המדווח — **נשארה פתוחה** | present | The README's own table (lines 109-110) and docs/SPEC.md:516,521 mark TWO entries STILL OPEN: 6.6 (line endings, empty file) and 6.7 (reportedVat). The pre-reconciliation README (git show a65a5b2:products/pcn874/README.md line 32) lists the seven as 6.1, 6.5, 6.2, 6.4, 6.6, 6.8 and 6.7. Of those, four are resolved by the document, one by the vendor manual and two remain open. Counting all eight, five are resolved by the document and two are open. | מתוך המחלוקות שנרשמו קודם: ארבע הוכרעו לפי המסמך (ועוד אחת, 6.3, שלא נספרה כמחלוקת), אחת לפי מדריך ספק חדש יותר, ושתיים נשארו פתוחות — סיומי שורה / קובץ בלי תנועות (6.6), והחישוב של הסכום המדווח (6.7). |
| **LOW** | UNSUPPORTED | 256 | מבקר חיצוני בדק את החבילה שורה מול שורה מול המסמך הרשמי. | present | The audit is research/colony-sweep/audits/pcn874-reconciliation.md, written by this repository's own audit process. Nothing in the repo says an outside party did it. The English text (line 46) says only "a refuter". | מבקר נפרד (סוכן ביקורת שלא בנה את החבילה) בדק את החבילה שורה מול שורה מול המסמך הרשמי. |
| **LOW** | UNSUPPORTED | 260 | רשות המסים מפרסמת סימולטור בחינם — יש להשתמש בו | present | Same as line 125: the URL is sourced from herp:1263 (a vendor manual), "free" is stated nowhere in the rendered sources, and herp:1269-1270 says the simulator checks only partially. | לרשות המסים יש סימולטור מקוון (הכתובת לפי מדריך H-ERP, שורה 1263); לפי אותו מדריך הוא בודק את הקובץ באופן חלקי בלבד. יש להשתמש בו: ... |

## `products/x402-il-api/README.md` — 90 claims checked, 15 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 118 | **The facilitator is an owner decision, not a default.** ... Before the first paid deploy, from a machine with egress: ... If that prints nothing, set `X402_FACILITATOR_URL` ... Paste the output into this section when known. | changed | The document contradicts itself. Line 87 says `**For x402: nothing.** No account, no KYC, no payout setup`, and lines 118-129 give the owner a decision plus a command to run before the first paid deploy. logs/CHECKPOINT.md:365-367 records it as an owner step ('ורשומה ב-README של המוצר כצעד בעלים', 'recorded in the product README as an owner step'). The step is not in the one checklist. src/revenue/owner-steps.ts has 7 ids (merge-pr, tax-file, gumroad, domain, github-org, algora-stripe, ci-tokens), and src/__tests__/revenue/owner-steps.test.ts:17-24 fails the build on an eighth. `grep -i 'x402\|facilitator\|wallet' docs/OWNER_STEPS.he.md src/revenue/owner-steps.ts` finds only OWNER_STEPS.he.md:271 (USDC cash-out, 'not now'). MISSION.md:322-323: 'Never invent a step that isn't required.' No paid deploy is planned: paid-apis and agent-services were killed 2026-09-07 (src/revenue/portfolio.ts:409-433), and the standby clause at :420-421 says 'nothing is planned on it'. 'No account' is also undercut by the README itself: line 38 sizes the price on the Coinbase CDP facilitator, and lines 62 and 128-129 plan for a facilitator that requires a bearer token (app.ts:70-74). | Replace lines 118-129: 'The facilitator is not chosen yet, and choosing it is agent work, not an owner step. The SDK defaults to https://x402.org/facilitator. Whether it settles exact on eip155:8453 is unchecked. The container cannot reach it, but render-watch.yml can: add https://x402.org/facilitator/supported to research/rendered/urls.txt and dispatch. The line is killed (portfolio.ts KILLED_LINES), so no paid deploy is planned. If a facilitator that needs an account (CDP) is ever chosen, that account becomes an owner step in docs/OWNER_STEPS.he.md, not here.' Line 87: 'For x402: nothing, and nothing is planned. The line is on standby. If it is ever re-opened, anything it needs from the owner goes into docs/OWNER_STEPS.he.md first.' |
| **MEDIUM** | STALE | 5 | Revenue lines: `paid-apis` and `agent-services` in the automaton's revenue colony. | present | src/revenue/portfolio.ts:409-433 lists both in KILLED_LINES with killedOn '2026-09-07' and targetMonthlyAgorot 0. The standby clause at :420-421 reads: 'products/x402-il-api stays deployed only while it costs ₪0/month, as a RAIL ON STANDBY rather than a line ... nothing is planned on it.' The DEFAULT_PORTFOLIO ids (:36,100,136,172,352,362) contain neither. logs/CHECKPOINT.md:245-247 agrees. | Revenue lines: none. The board killed `paid-apis` and `agent-services` on 7.9.2026 (src/revenue/portfolio.ts KILLED_LINES). This API is a rail on standby. It is kept only while it costs ₪0/month, and nothing is planned on it. |
| **MEDIUM** | STALE | 9 | x402 is the only rail in the portfolio that needs **no account and no KYC from the owner**. ... That makes it the first thing that can earn while every other line is still waiting on a one-time signup. | present | x402 is no longer in the portfolio: LINE_RAILS in src/revenue/rails.ts has payins apify/gumroad/gumroad/bounty-platform (:72,79,86,93), and 'x402' survives only in the type union at :30. The line was killed at about ₪6 per provider per month (portfolio.ts:416). logs/CHECKPOINT.md:257-259 records that agent-services used to 'cover' for the owner-blocked lines and now does not. apify-actors also publishes free with 'no KYC and no payout method' (rails.ts:74), so x402 is not the only no-KYC path. | Why it is kept: x402 needs no account and no KYC to receive USDC. That is why this API stays up on standby at ₪0/month. It is not a line: the board killed it on 7.9.2026 because measured demand divides out to about ₪6 per provider per month. |
| **MEDIUM** | STALE | 44 | npm test          # 22 tests, including the real paywall factory against the real v2 middleware | present | `grep -cE "^\s*it\(" tests/api.test.ts` gives 37, and a grep for .skip/.only/.each/.todo finds none. Per describe: 3+6+3+13+2+3+5+2 = 37. logs/2026-09-05-x402-v2-migration-and-vertical-niches.md:24 records '15 → 35 בדיקות' ('15 to 35 tests'); :36 shows 22/22 was the interim pre-fix commit. The openapi describe (tests :484-504, 'The judge's last ask') brings the count to 37. I did not run the suite: the product has no node_modules, the root lacks supertest/express/jsonrepair/@hebcal/core/@x402, and installing needs the registry. | npm test          # 37 tests, including the real paywall factory against the real v2 middleware (or drop the count: '# full suite; paid-mode tests run the real @x402 middleware') |
| **MEDIUM** | UNSUPPORTED | 87 | earnings accrue as USDC in the wallet the automaton already controls (and line 9: Payments land directly in the automaton's own wallet.) | present | Payments go to X402_PAY_TO (src/config.ts:133; src/app.ts:183). Nothing sets it to the runtime's wallet: grep -rn X402_PAY_TO outside products/x402-il-api/{src,tests} finds only README lines 54/59/135 and a sibling audit. The runtime wallet lives at ~/.automaton/wallet.json (src/identity/wallet.ts:44-48). `ls ~/.automaton` gives 'No such file or directory', and the loop runs 'בלי ארנק' ('without a wallet', logs/CHECKPOINT.md:542). No deploy of this API is recorded anywhere in the repo. Sibling audit research/owner-docs-audit/products-readme.md F6 found that no code moves inbound USDC into transactions or revenue_ledger. | Line 9: 'Payments settle to the address in X402_PAY_TO.' Line 87: 'Earnings would accrue as USDC at X402_PAY_TO. Nothing sets that to the runtime's own wallet yet, and nothing has been deployed. USDC that arrives is booked by hand (scripts/tag-payment.md), not automatically.' |
| **MEDIUM** | STALE | 121 | Whether it lists Base mainnet cannot be checked from this repo's sandbox. | present | This was true of the container on 5.9.2026. Since 7.9.2026 the repo has .github/workflows/render-watch.yml:1-27, built for pages 'egress-blocked from the container ... GitHub's runners have egress'. It fetches JSON too: research/rendered/apify-store-accessibility.meta.json has url https://api.apify.com/v2/store?search=accessibility, contentType 'application/json; charset=utf-8', status 200, fetchedAt 2026-09-07T17:15:55Z, and scripts/render-watch.mjs:185 picks json from the content type. urls.txt requires a URL to appear verbatim in a repo file (research/rendered/urls.txt:7-10), and README line 125 already contains https://x402.org/facilitator/supported. | This container cannot reach it, but .github/workflows/render-watch.yml can: add https://x402.org/facilitator/supported to research/rendered/urls.txt, dispatch the workflow, and read research/rendered/<slug>.json. |
| **MEDIUM** | WRONG | 125 | curl -s https://x402.org/facilitator/supported \| jq '.kinds[] \| select(.network=="eip155:8453")'  (with line 128: If that prints nothing, set X402_FACILITATOR_URL ...) | present | The startup gate requires three fields. src/app.ts:98 is `kinds.some((k) => k.x402Version === 2 && k.scheme === "exact" && k.network === config.network)`, and src/index.ts:174-179 calls process.exit(1) when it is false. The jq filter checks only network. A facilitator listing eip155:8453 only for x402 v1, or only for a non-exact scheme, prints a kind: the reader concludes the default works, and the paid deploy then refuses to start. tests/api.test.ts:418-427 shows 'unsupported' is decided on these fields. | curl -s https://x402.org/facilitator/supported \| jq '.kinds[] \| select(.x402Version==2 and .scheme=="exact" and .network=="eip155:8453")'. It must print exactly the kind index.ts looks for. If it prints nothing, the paid deploy will refuse to start on this facilitator. |
| **LOW** | WRONG | 11 | This line is deliberately cheap to run and shares its domain logic with the Apify actor line rather than being a standalone bet. | present | The Apify line (apify-actors, portfolio.ts:36-42) publishes products/apify-il-open-data, a data.gov.il CKAN wrapper (src/ckan.ts, csv.ts, dictionary.ts, normalize.ts...). `grep -rln "israeli\|teudat\|zehut" products/apify-il-open-data/src products/apify-il-open-data/test` finds nothing. The logic is shared with products/mcp-il-tools instead: `cmp mcp-il-tools/src/israeli.ts x402-il-api/src/israeli.ts` reports them identical, and mcp-il-tools/tests/server.test.ts:98-105 enforces it. | ...and shares its validators with products/mcp-il-tools (a byte-identical copy of src/israeli.ts, enforced by that package's tests) rather than being a standalone bet. |
| **LOW** | WRONG | 19 | \| GET \| `/health` \| Liveness, and whether the paywall is armed \| | present | src/app.ts:217-219 returns paywall: config.paywallEnabled ? 'x402' : 'free', where paywallEnabled = Boolean(payTo) (config.ts:145). It reflects configuration only. tests/api.test.ts:346-368 has an unreachable facilitator: /v1 answers 500/502 while /health is 200 and, by the code, says 'x402'. Tests :385-396 have the packages missing: /v1 answers 503, /health 200. | \| GET \| `/health` \| Liveness, and whether paid mode is configured (X402_PAY_TO set). It does not show whether a paid request can currently settle. \| |
| **LOW** | WRONG | 28 | \| POST \| `/v1/validate/phone` \| Israeli phone validation, type, and E.164 form \| | present | src/israeli.ts:53-54 returns toll_free (1-800), national (1-700) and premium (1-900) with a national form and no e164. e164 is built only for mobile, landline and VoIP (:56-59). openapi.yaml:108 agrees: 'Absent for premium 1-700/800/900 numbers'. | Israeli phone validation and type, plus the E.164 form for mobile, landline and VoIP numbers (1-700/1-800/1-900 get a national form only) |
| **LOW** | WRONG | 60 | `base` and `base-sepolia` are mapped; anything else must be CAIP-2 (also line 102-104: anything else must already be CAIP-2 or the process refuses to start) | present | src/config.ts:58 has EVM_CAIP2 = /^eip155:\d+$/, and :64-69 throws otherwise. tests/api.test.ts:450 refuses 'solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp', a valid CAIP-2 id. README line 138 states the rule correctly. | anything else must be an EVM CAIP-2 id, eip155:<chainId>. Any other value, including a non-EVM CAIP-2 id such as solana:..., is refused at startup. |
| **LOW** | UNSUPPORTED | 67 | Deploy on any Node 22.12+ host (the runtime itself runs on 20, the test runner does not) | present | package.json:27-29 declares engines node '>=22.12' for the package. CI runs Node 22 only (.github/workflows/products-ci.yml:24-26), and nothing starts the server on 20. The lockfile does not contradict the claim, since no runtime package declares a floor above 18, but it is untested. The test-runner half is correct: vitest 5.0.0 engines are '^22.12.0 \|\| ^24.0.0 \|\| >=26.0.0'. | Deploy on any Node 22.12+ host (package.json engines; CI runs 22). No runtime dependency excludes Node 20, but nothing here tests it. |
| **LOW** | STALE | 148 | 22 tests. | present | Same as line 44: tests/api.test.ts contains 37 it() calls and no modifiers. 22 was the interim count in logs/2026-09-05-...:36, superseded in the same session by 35 (:24) and later by 37. | 37 tests. (or remove the count) |
| **LOW** | UNSUPPORTED | 152 | `x402-express@1.2.0` pulled `x402` → `wagmi` → `porto` → `react` and `@tanstack/react-query`. ... a React 18-vs-19 peer conflict ... (and 158-160) remove vitest and it installs; keep vitest and pass `--legacy-peer-deps` and it installs | present | The pre-migration lockfile is not in the repo: `git rev-parse --is-shallow-repository` returns true and history starts at a65a5b2 (2026-09-07). logs/2026-09-05-...:66 backs the removal of wagmi/react/porto/x402-express, and :54-55 backs the edgesOut crash and the vitest-5 fix. Nothing records the React 18-vs-19 conflict, @tanstack/react-query, the --legacy-peer-deps result, or 'remove vitest and it installs'. None of it can be re-run without the registry. No owner action depends on it. | Keep it as history and mark it: '(recorded in logs/2026-09-05-x402-v2-migration-and-vertical-niches.md; the pre-migration lockfile is no longer in the repo)'. |
| **LOW** | STALE | 161 | The other four products are still on 4.1.11 and unaffected until they take an `@x402` dependency. | present | There are five other products (.github/workflows/products-ci.yml:18). `grep '"vitest"' products/*/package.json` gives apify-il-open-data 3.2.7, il-biz-tools 4.1.11, mcp-il-tools 4.1.11, pcn874 5.0.0, telegram-il-tools-bot 4.1.11. pcn874 was added 7.9.2026 'בסגנון x402-il-api' ('in the style of x402-il-api', logs/2026-09-07-pcn874-validator.md:86). | Of the other five products, three are on vitest 4.1.11 (il-biz-tools, mcp-il-tools, telegram-il-tools-bot), apify-il-open-data is on 3.2.7 and pcn874 is on 5.0.0. None has an @x402 dependency, so the crash does not reach them. |

## `products/mcp-il-tools/README.md` — 48 claims checked, 11 findings

| Impact | Status | Line | Claim (verbatim) | Now | Evidence | Proposed fix |
|---|---|---|---|---|---|---|
| **HIGH** | WRONG | 14 | "il-tools": { "command": "npx", "args": ["-y", "@bediyuk/mcp-il-tools"] } | present | Launched through this config, the server never serves MCP. src/server.ts:123 starts the transport only if import.meta.url === `file://${process.argv[1]}`. npm installs the bin as a symlink (node_modules/.bin/mcp-il-tools -> ../@bediyuk/mcp-il-tools/dist/server.js; the npx cache has the same symlink at /root/.npm/_npx/464f7129852fbcbe/...). Node puts the symlink path in argv[1] and the resolved path in import.meta.url, so the guard is false. Minimal reproduction: via a symlink the guard prints "guard":false. End to end with the built and packed package (npm pack → bediyuk-mcp-il-tools-0.1.0.tgz) and an MCP initialize request on stdin: `node dist/server.js` answers with serverInfo il-tools 0.1.0, but `node_modules/.bin/mcp-il-tools` and `npx -y --package=<tgz> mcp-il-tools` both print nothing and exit 0. Tests miss this because they import buildServer() over an in-memory transport (tests/server.test.ts:5,13-15). BOARD.md:74 makes registry installs the KPI of this channel test, so every install would fail silently. | Fix the code before publishing: guard with realpathSync(process.argv[1]) === fileURLToPath(import.meta.url), which was verified true both directly and through a symlink, or use a bin entry that always connects. Add a test that runs the packed bin through node_modules/.bin. README until then: "**Not installable yet.** The package is not on npm, and the published entry point does not yet start when launched through npx (fix pending in src/server.ts). The config below is what it will be:" |
| **MEDIUM** | UNSUPPORTED | 11 | ## Install ... npx -y @bediyuk/mcp-il-tools (presented as installable from npm) | changed | Nothing publishes the package. .github/workflows has no npm publish or mcp-publisher step (grep → nothing), and release.yml:13-17 only builds the root package. No NPM_TOKEN or npm-account step exists (owner-steps.ts:42-49 has seven step ids, none of them npm). logs/CHECKPOINT.md:555 says the @bediyuk scope has no packages. The planned .github/workflows/mcp-publish.yml (BOARD.md:74,243) does not exist. | Put this above the config block: "Not yet published to npm (no publish workflow or npm account exists yet; see products/README.md)." |
| **MEDIUM** | UNSUPPORTED | 19 | Registry name: `com.bediyuk/il-tools`. | present | The name matches server.json:3, but it is written as settled and it is a plan. The listing is blocked on step 5 (domain) and step 7 (organisation): products/README.md:10,74-75 and BOARD.md:243. The brand is not confirmed: CHECKPOINT.md:555 reads 'לא אושר עדיין' ('not confirmed yet'), and OWNER_STEPS.he.md:167 still says a domain name will be sent. The namespace needs DNS proof for bediyuk.com (MISSION.md:283-286), which has not been bought. The registry quickstart recorded in the repo (scouts/distribution--partnerships-integrations.md:14) requires mcpName in package.json, and `grep -c mcpName package.json` returns 0. | "Planned registry name: com.bediyuk/il-tools — not listed. Listing needs the brand domain (owner step 5, for DNS proof of the namespace), the GitHub organisation (step 7), the package on npm, and \"mcpName\": \"com.bediyuk/il-tools\" in package.json. The brand name is not confirmed; if it changes, the namespace, npm scope and server.json change with it." |
| **MEDIUM** | WRONG | 19 | server.json (in audit scope): "websiteUrl": "https://bediyuk.co.il" alongside namespace com.bediyuk | changed | server.json:5 contradicts the board's ruling in docs/OWNER_STEPS.he.md:168 ('פסק הדירקטוריון: סיומת `.com`, לא `.co.il`', i.e. 'the board's ruling: a .com suffix, not .co.il'). owner-steps.ts:128 agrees ('.co.il only if ISOC-IL privacy is confirmed first'). It also contradicts the com.bediyuk namespace in the same file, which is the reverse-DNS of bediyuk.com. `grep -rn bediyuk\.co` finds only this line. It points at a domain nobody has bought. | Remove websiteUrl until the domain is bought, then set it to the purchased .com domain. The line-19 note should say server.json follows the domain. |
| **MEDIUM** | WRONG | 28 | `hebrew_date` \| What is this Gregorian date in the Hebrew calendar, and is it a Hebrew leap year? | present | On any host west of UTC the tool returns the previous Hebrew day. israeli.ts:113 builds new Date(Date.UTC(y,mo-1,d)) and :117 passes it to new HDate(date). @hebcal/hdate greg.js:93 then reads the local fields date.getFullYear()/getMonth()/getDate(). The server runs on the caller's machine (README line 7). Scratch run of the built dist: UTC 2026-09-12 → 1st of Tishrei 5787; America/New_York 2026-09-12 → 29th of Elul 5786 (wrong day and wrong year, and isLeapYear is computed from that year); America/Los_Angeles 2026-09-03 → 20th of Elul, where UTC gives the 21st. The test only asserts year > 5780 (tests/server.test.ts:75), and CI runs in UTC. | Code: new HDate(new Date(y, mo - 1, d)) (local midnight), plus a test under TZ=America/New_York. README until then: "hebrew_date … Known defect: on a machine west of UTC it currently returns the previous Hebrew day." |
| **MEDIUM** | STALE | 53 | The same logic is sold per-call over the x402 protocol in products/x402-il-api ... The paid API exists for callers who want an HTTP endpoint instead of a dependency. | changed | Both x402 lines were killed on 2026-09-07 (src/revenue/portfolio.ts:410-433). The standby clause (:420-421) says x402-il-api is a 'RAIL ON STANDBY … nothing is planned on it'. No deploy or URL is recorded anywhere (grep; sibling audit x402-il-api.md:166). The agent-services kill reason is that it 'fails never sell what is already free: the Israeli identifier detector it would meter exists free inside this repository' (portfolio.ts:430), and MISSION.md:344-345 agrees. This section still argues the pre-7.9 position. Also, the relative link ../x402-il-api would be dead on npm: README.md is in the npm pack file list and package.json has no repository field. | "## The HTTP version — The same validators also exist as an HTTP API over x402 inside this repository (products/x402-il-api). It is not deployed and it is not a revenue line: the board killed both x402 lines on 7.9.2026, in part because it would charge for what this package gives away. This package is the complete product: identical validators, no rate limit, no telemetry." Drop the relative link, or make it absolute once the repo is under the organisation. |
| **LOW** | UNSUPPORTED | 25 | Pads to nine digits first, which is the step most implementations skip. (and line 3: 'the Israeli data checks that agents get wrong') | present | Nothing in the repo surveys other implementations or agent behaviour. The tool's own description (server.ts:44) says 'get wrong', not 'skip'. | Line 3: "An MCP server for Israeli data checks that are easy to get wrong: …"; line 25: "Pads to nine digits first, a step that is easy to miss." |
| **LOW** | UNSUPPORTED | 29 | Latin transcription of Hebrew text, for slugs and filenames. | present | Probe: transliterate_hebrew 'שלום עולם' → "shlvm 'vlm"; 'אבא' → "'v'". israeli.ts:126,128 map א and ע to an apostrophe, and :134-136 keep whitespace, so the output is not slug-safe. | "Latin transcription of Hebrew text, as a starting point for slugs and filenames (it keeps spaces and renders א/ע as an apostrophe). Approximate by design." |
| **LOW** | WRONG | 31 | Each returns JSON. Invalid input comes back as a result explaining why, not an exception — ... an agent can act on `{"valid": false, "reason": "..."}`. | present | In-memory MCP client probe. validate_israeli_id {id:12345} → isError=true with the plain text 'MCP error -32602: Input validation error: … expected string, received number at id', which is not JSON. server.ts:46 declares z.string() although israeli.ts:10 accepts numbers; the phone tool is the same (server.ts:60). hebrew_date {date:'2026-02-30'} → {"error":"bad_request","message":"invalid calendar date"}, not the valid/reason shape. | "Each tool returns JSON. A malformed value comes back as a JSON result that says why: {\"valid\": false, \"reason\": \"...\"} from the validators, {\"error\": \"bad_request\", \"message\": \"...\"} from hebrew_date. An argument of the wrong type (e.g. an ID sent as a number) is rejected by the MCP SDK as a plain-text tool error." |
| **LOW** | UNSUPPORTED | 46 | Bank validation is structural. It checks the code, branch and account shape and names the bank. | present | The disclosure is incomplete. israeli.ts:62 says 'this list covers the major banks' and :63-82 hard-codes 18 codes. Any other code returns valid:false with 'unknown Israeli bank code N' (:98), so a real bank that is not in the table is reported as not plausible. Neither line 27 nor this 'honest about' section says so. | Append: "The bank list is a fixed table of 18 codes; a code outside it is reported invalid even if the bank exists." |
| **LOW** | WRONG | 56 | identical validators, no rate limit, no telemetry, nothing withheld | present | Read literally, it is wrong. The paid API's price list has a sixth billable endpoint, POST /v1/json/repair (x402-il-api/src/config.ts:158, app.ts:276), and this package does not have it. All five Israeli validators are here and byte-identical (cmp exit 0), so the narrow reading holds. | "identical Israeli validators, no rate limit, no telemetry, none of them withheld (the paid API's one extra endpoint, JSON repair, is unrelated and not part of this package)". This goes away if the line-53 rewrite is adopted. |
