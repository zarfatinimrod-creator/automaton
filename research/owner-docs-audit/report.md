# Claim check — state/colony/REPORT.md

The checker returned its findings but wrote no file; its note: "NOT WRITTEN. The harness refused a subagent write to /home/user/automaton/research/owner-docs-audit/report.md ("Subagents should return findings as text"). The full audit is in these findings.  Method: every tool run used a byte-identical copy of state/colony/colony.db in the scratchpad. sha256 b2d3f67e...ffb42 was unchanged on the original. The only repo change is the pre-existing untracked research/owner-docs-audit/ directory.  Reproduction: - `status` on the copy matches the document: ₪0.00 (0.0%), target ₪20,000, stretch ₪50,000, four awaiting_setup lines at ₪200/₪400/₪300/₪600, all four with supervisor escalate, and killed:6. - `report --now 2026-09-25T13:44:58.623Z`, with the connector env vars unset so nothing touched the network, matches byte for byte except the "This tick" block. The kv already records the 13:44 sync. - `report` at 16:38Z matches except the Generated line and "This tick"; the supervisor review is now due. - The ledger has 0 rows, so no number exceeds it. - vitest on runner, target-basis, owner-steps and colony-cli: 4 files, 58 tests passed.  52 claims checked: 43 CORRECT, 2 OUT OF SCOPE (external-platform claims with no research/rendered source: the Apify Store URL is public; the Gumroad bank holder name must be in Latin characters), 7 non-correct (F1-F7). All 7 come from the generator (src/revenue/runner.ts:458-477 and humanSetup in src/revenue/portfolio.ts), so editing REPORT.md alone would be overwritten on the next tick.". Findings, verbatim from its structured result:

- **WRONG / HIGH** (line 54): Sign up at Apify with the brand as the username ... and paste APIFY_TOKEN as a GitHub Actions secret (owner step 6; this half may be done straight after step 1). Nothing else: publishing free and counting stranger runs needs no identity verification.
  - Evidence: The same text also appears in Blocked on (line 46). The CI push creates the Actor PRIVATE, and making it public takes a one-time Console click by the owner. Sources: (1) .github/workflows/apify-publish.yml:180-183: '`apify push` creates or updates the Actor and its build. Making it PUBLIC in the Store is a one-click step in the Console the first time (Publication -> Publish to Store); ... this workflow deliberately does not touch that surface.' (2) products/apify-il-open-data/README.md:141,149-150, under 'this is the whole list': '3. The first push creates the Actor privately. Open it in the Console once and press Publication → Publish to Store to make it visible. One click, once.' (3) docs/OWNER_STEPS.he.md:219-221, step 6 part ג, describes the same click. Consequence: with the token pasted and no click, the Actor stays private and the stranger count reads 0. That trips the kill criterion at src/revenue/portfolio.ts:73: 'strangerUsers30d under 10 at day 30 → instrument only ... permanently unless the count later crosses 50'.
  - Proposed fix: Change humanSetup at src/revenue/portfolio.ts:95 to: 'Sign up at Apify with the brand as the username and paste APIFY_TOKEN as a GitHub Actions secret (owner step 6, early half; step 1 is already done, so this can be done now). After the first CI push, open the Actor in the Apify Console once and press Publication → Publish to Store: the push creates it private, and a private Actor cannot be counted. Neither action needs identity verification.'
- **WRONG / HIGH** (line 51): ## What the owner has to do (one time, per line) — [per-line lists at lines 53-67, repeated in Blocked on at lines 46-49, followed by 'When a line's steps are done, confirm it']
  - Evidence: Owner step 2 (tax-file: עוסק פטור + ביטוח לאומי) appears nowhere in REPORT.md. src/revenue/owner-steps.ts:95-107 gives it lines [apify-actors, il-biz-tools, oss-bounties, pcn874] and unlocks: 'The legal right to receive any shekel at all ... business income needs a file at the Tax Authority, and ₪10 counts.' The report also never points to docs/OWNER_STEPS.he.md, which MISSION.md:322 names as the one ordered checklist.  How it happened: portfolio.ts:93 took step 2 out of every humanSetup ('it is owner step 2, once'), following BOARD.md:99. renderReport (runner.ts:458-466) prints only humanSetup, so step 2 went from once to zero times.  Computed with ownerStepsForLine() against the steps cited in each humanSetup (scratch tsx script): - apify-actors: gating [1,2,6], cited [6] - il-biz-tools: gating [1,2,3,5,6], cited [3,5,6] - oss-bounties: gating [1,2,7,4,6], cited [7,4] - pcn874: gating [1,2,3,5,7,6], cited [3,7]  Step 1 is already done (31cda66 merged PR #2 on 2026-09-22). Leaving step 2 out for apify-actors is defensible because free publishing takes no money. For il-biz-tools, pcn874 and oss-bounties, which sell through Gumroad or Stripe, it is not. No test checks this: owner-steps.test.ts:94-103 only requires at least one step per line.
  - Proposed fix: Build each line's list in renderReport (src/revenue/runner.ts:458-466) from ownerStepsForLine(line.id). Or print this above the per-line lists: 'Every line that takes money is also blocked on owner step 2 (עוסק פטור file + ביטוח לאומי), listed once. The single ordered checklist is docs/OWNER_STEPS.he.md: 1 → 2 → 3 → 5 → 7 → 4 → 6, and step 1 was done on 22.9.2026.'
- **WRONG / MEDIUM** (line 67): pcn874: [Gumroad (owner step 3)]; [GitHub organisation (owner step 7)] — and oss-bounties (lines 62-63): [machine account (owner step 7)]; [Algora/Stripe (owner step 4, done after step 7)] — presented as each line's complete setup
  - Evidence: pcn874 is also blocked on two steps the report omits: - step 5, domain (owner-steps.ts:129 lines [il-biz-tools, pcn874]) - step 6, ci-tokens: Netlify plus GUMROAD_ACCESS_TOKEN (owner-steps.ts:166). Without that token no Gumroad sale reaches the ledger (colony.yml:53-57; connectors/gumroad.ts:14), and pcn874 sells through Gumroad (portfolio.ts:181).  oss-bounties is also blocked on step 6 (owner-steps.ts:166), whose unlocks text says 'BRAND_GITHUB_TOKEN lets bounty PRs leave the brand account' (:165). src/revenue/bounties/intake.ts:13-16: 'the line is blocked on owner steps 7 ... and 4 ... and on `BRAND_GITHUB_TOKEN`, and a loop that attempted a bounty before those exist would publish a pull request under the owner's handle.' BOARD.md:201 puts BRAND_GITHUB_TOKEN in step 6b.  Line 69 tells the owner to confirm setup-done once the listed steps are done, which would release both lines too early.
  - Proposed fix: Add to pcn874's humanSetup (portfolio.ts:197-200): 'Buy the domain (owner step 5) and paste GUMROAD_ACCESS_TOKEN as an Actions secret (owner step 6), both shared with il-biz-tools.' Add to oss-bounties' humanSetup (portfolio.ts:165-168): 'Paste the brand machine account's token as BRAND_GITHUB_TOKEN (owner step 6; Claude will ask when the code that uses it exists). Do not confirm this line before then.' Alternatively, the generator change proposed in the owner step 2 finding fixes both lines.
- **WRONG / MEDIUM** (line 69): When a line's steps are done, confirm it so the colony can start building: pnpm exec tsx scripts/colony.ts setup-done apify-actors --evidence "done on <date>"
  - Evidence: (1) The colony loop cannot build. setup-done only sets the line to proposed and queues a build goal (scripts/colony.ts:305-307). The scheduled loop runs `tick --no-feed` (colony.yml:61), so the board review records 'goal filing disabled for this review (no executor attached)' (heartbeat.ts:351-355) and never files the goal. runner.ts:9-11: 'Director and worker EXECUTION still needs inference.' colony.yml:59-60: 'Governance runs; execution waits.' Building is done by Claude sessions, and has happened already without any confirmation: products/pcn874, products/il-biz-tools and products/apify-il-open-data exist while all four lines are awaiting_setup.  (2) The owner is told elsewhere to report back rather than run a command. docs/OWNER_STEPS.he.md:224 says 'תגיד לי "צעד 6 בוצע..."', the summary table has a 'מה לשלוח לי' column (:297), and logs/CHECKPOINT.md:683 has the agent run setup-done. Run on the owner's machine, the command writes only to his local state/colony/colony.db (default --db, colony.ts:49). The loop reads the copy committed on main, which colony-bot re-commits after every tick.
  - Proposed fix: Replace runner.ts:467-471 with: 'When a line's steps are done, tell Claude which steps and when. Claude records it (pnpm exec tsx scripts/colony.ts setup-done <line> --evidence "...") and commits the state. That releases the line from awaiting_setup and queues its build goal. The scheduled loop does not build; a Claude session does.'
- **WRONG / LOW** (line 54): Apify KYC and a PayPal payout are deferred until stranger runs exist.
  - Evidence: Also on line 46. The repo's threshold is 50 stranger users in 30 days, not 'stranger runs exist': - portfolio.ts:80: 'strangerUsers30d at or above 50 → one more Actor ... and Apify KYC goes to the owner' - BOARD.md:328: 'Second Apify Actor / Apify KYC / Apify pricing \| 50 / 50 / 200 stranger users' - Under 10 at day 30, KYC is never asked for (portfolio.ts:73). The payout rail is 'PayPal or Wise' (portfolio.ts:282), not PayPal only.
  - Proposed fix: 'Apify KYC and a PayPal or Wise payout are deferred until 50 stranger users in 30 days. If fewer than 10 by day 30, they are never asked for.'
- **UNSUPPORTED / LOW** (line 67): Create the GitHub organisation under the brand name so the open-source core and the npm scope carry it and not your username (owner step 7)
  - Evidence: Also on line 49. Nothing in the repo creates or owns an npm scope: - `grep -rn NPM_TOKEN` over the repo (node_modules excluded) returns nothing. - No workflow publishes to npm. - No owner step covers an npm account. - products/pcn874/package.json is "name": "pcn874", "private": true. The only mentions are the plan text itself (portfolio.ts:180,199,324; BOARD.md:72). No repo source says the GitHub org (step 7) gives the brand an npm scope. If a separate npm account is needed, it would be an eighth owner step, which owner-steps.test.ts forbids.
  - Proposed fix: 'Create the GitHub organisation under the brand name so the open-source core carries it and not your username (owner step 7).' Drop the npm scope from the owner's text until the repo shows how it is created and by whom.
- **WRONG / LOW** (line 77): Money here is only what reached the ledger with a platform transaction id.
  - Evidence: This holds for sale, subscription, payout and refund (ledger.ts:408-414). A cost may be recorded without an id: ledger.ts:412 says 'Only 'cost' may omit it', and scripts/colony.ts:99-101,265 allows it from the CLI. The Costs (30d) and Net (30d) rows on this page can therefore include hand-entered money with no platform id. The effect today is nil because there are no cost rows.
  - Proposed fix: Change runner.ts:477 to: 'Revenue here is only what reached the ledger with a platform transaction id; costs may be entered by hand without one. Projections are never counted.'
- **CORRECT / LOW** (line 40): Skipped as not yet due: revenue_supervisor_review, revenue_board_review, revenue_audit
  - Evidence: Supervisor review last ran at kv revenue.last_run.revenue_supervisor_review = 2026-09-25T07:45:57.054Z, on a 6 h interval (heartbeat.ts:55), so it was due at 13:45:57Z. The tick ran at 13:44:58.623Z, 58 s early. Board review last ran 2026-09-24T22:22:49Z (24 h interval); audit 2026-09-22T18:40Z (7 d). Re-running `report --now 2026-09-25T13:44:58.623Z` on a copy reproduces everything outside this block byte for byte. Re-running at 16:38Z runs the supervisor review, so this block is a snapshot and will change on the next tick.
  - Proposed fix: No change needed.
- **CORRECT / LOW** (line 42): Ledger sync: 0 new entries, 0 already known, sources [none configured]
  - Evidence: kv revenue.last_ledger_sync = {recorded:0, duplicates:0, sources:[], errors:[]}. One nuance: the local x402 reader did run (kv revenue.connector_cursor.x402 was rewritten at 13:44:58). heartbeat.ts:137 lists it under sources only when it finds entries. Remote connectors appear only when their key is set (heartbeat.ts:143; colony.yml:55-57). 'None configured' is accurate for the remote sources.
  - Proposed fix: Optional: 'sources [no platform keys set; local x402 reader found nothing]'.
- **CORRECT / LOW** (line 20): Line targets, summed \| ₪1,500 against a ₪20,000 goal (and measured ₪0 / inferred ₪1,100 / unevidenced ₪0 / contradicted ₪400)
  - Evidence: This table is computed from code (summarizeTargetBasis over DEFAULT_PORTFOLIO and TARGET_BASIS, runner.ts:367), not from the database. The two agree today: - Seeds: portfolio.ts:87,126,163,195 give 200+400+300+600. - Database: target_monthly_agorot is 20000/40000/30000/60000. - Grades: apify inferred (:277), oss inferred (:305), pcn874 inferred (:318), il-biz-tools contradicted (:290). - The apify 'inferred' grade is the board's ruling (research/colony-sweep/BOARD.md:99). If a database target ever drifts from its seed, this table and the Revenue lines table would disagree silently.
  - Proposed fix: No change needed.
- **CORRECT / LOW** (line 54): (owner step 6; this half may be done straight after step 1)
  - Evidence: Also on line 46. Matches owner-steps.ts:168-173 (earlyPart.afterStep 'merge-pr'). Step 1 is already done: commit 31cda66 'Merge pull request #2' by the owner on 2026-09-22, and colony.yml is on origin/main (the colony-bot tick commits are on main). The page does not say the Apify half can be done now.
  - Proposed fix: Optional: '(owner step 6; step 1 is done, so this half can be done now)'.
